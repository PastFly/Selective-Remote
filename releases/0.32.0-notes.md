# Selective Remote 0.32.0 (163)

Prepared from the verified `v0.31.0..1800be3246f41dbecae98b5497f3ba83ec6673e4` source range. These notes describe source capabilities; the official 0.32.0 artifact and release acceptance are still pending.

## FINAL_DETAILED_RELEASE_NOTES_RU

### Новое в 0.32.0

Selective Remote Cloud впервые входит в публичный релиз. Подключения и рабочие данные теперь доступны на Mac и в браузере через личное хранилище со сквозным шифрованием. Команды могут вести общие Hosts, Credentials и Snippets в отдельных Team Vaults. Приложение по-прежнему можно использовать локально без аккаунта.

### Личный Vault и синхронизация

- Браузерный Personal Vault хранит Hosts, Credentials и Snippets в зашифрованном виде на стороне клиента. Изменения передаются как зашифрованные версии с явной обработкой конфликтов.
- macOS может создать Personal Vault, безопасно загрузить существующие данные после входа и автоматически синхронизировать поддерживаемые ресурсы в обе стороны.
- Личные Credentials и поддерживаемые файлы SSH-ключей синхронизируются в зашифрованном виде; открытые секреты не публикуются в Cloud.
- Данные браузера разделены по аккаунтам. На доверенном браузере разблокированный Vault восстанавливается после обычного входа и перезагрузки страницы без второго запроса пароля Vault.
- Если ключевой материал недоступен или версии конфликтуют, клиент показывает состояние восстановления и не заменяет данные молча.

### Команды, доступ и устройства

- Team Vaults дают отдельное общее пространство для Hosts, Credentials и Snippets. Поддерживаются роли Owner, Admin, Member и Viewer, приглашения и управление участниками.
- Доступ новых устройств требует допуска. Шифрование и расшифрование выполняются клиентами; сервер хранит зашифрованные данные и обёртки ключей для допущенных устройств.
- Удаление участника или устройства и смена ключевого поколения блокируют небезопасные записи до завершения необходимых обёрток. Клиенты показывают состояния конфликта, отзыва и отсутствующего ключевого материала.
- Личные данные и Keychain Mac отделены от Team Vault. Team-сессии не подхватывают личные учётные данные без явного выбора.
- Браузерные Team Vault metadata теперь совместимы с текущими macOS-клиентами; изменения записей сохраняют совместимые поля.

### Hosts и подключение

- Personal и Team Hosts получили согласованные папки, поиск, сортировку, список и плитку. Вложенные папки, перенос и изменение порядка работают в соответствующей области данных.
- Постоянная Host Shelf даёт быстрый доступ к выбранным Hosts; каталог можно свернуть, чтобы освободить место для текущей работы.
- Перетаскивание Team Host между папками одного Vault сохраняет новые настройки Host после синхронизации. Перенос между разными Vault запрещён.
- Выбор Host, Target и Vault стал поисковым. Фильтры Team Vault учитывают выбор пользователя и восстанавливаются после повторного запуска.
- SSH-пароль запрашивается один раз на попытку подключения; сохранённые учётные данные используются повторно там, где они подходят. Отмена запроса прекращает соответствующую попытку.
- Учётные данные управляемого Jump Host отделены от учётных данных конечного SSH-хоста. Исправлен транспорт управляемого Jump Host; прямой SSH и реальный маршрут через Jump Host проверялись отдельно до merge.
- Поддерживаемые SSH/SFTP-сценарии доступны и для Team Hosts с учётом роли, Vault и границы личных/командных секретов.

### Credentials, Snippets и рабочие области

- Team Credentials и Team Snippets имеют собственные общие библиотеки; личные библиотеки остаются в Personal Vault. Вложенные папки, поиск и редактирование Snippets стали согласованнее между Personal и Team.
- Улучшены компактные раскладки Hosts, Terminal, SFTP, Forwarding и Tunnels. Действия остаются доступны через компактные панели и меню при уменьшении окна.
- Активное состояние Terminal читается в светлой теме. RU/EN локализация охватывает Cloud, настройки, управление ресурсами, подсказки и затронутые экраны macOS.
- Окно «Что нового» представляет Cloud как новую публичную возможность. Командные предупреждения яснее объясняют область доступа и действие.

### Надёжность и безопасность

- Удаление Personal Host больше не подтверждается при ошибке удаления его учётных данных из Keychain: профиль и метаданные сохраняются, а пользователь получает ошибку.
- Экспорт зашифрованной резервной копии с учётными данными прерывается до записи архива, если миграция старых учётных данных не завершилась. Ранее существующий архив сохраняется.
- Обновление приложения проверяет закреплённого издателя перед установкой и повторно проверяет транзакционную копию до удаления резервной копии для отката.
- Путь официального релиза требует Developer ID Application, соответствующий Team ID, нотариализацию, stapling и проверку Gatekeeper. Эти требования описывают барьеры публикации, а не готовность 0.32.0 к выпуску.
- Ускорена обратная связь CI: Release compilation выполняется параллельно тестовому job. Это изменение процесса, а не характеристика производительности приложения.

### Ограничения перед выпуском

Официальный подписанный и нотариализованный DMG 0.32.0, приёмка Gatekeeper, чистой установки и обновления ещё не подтверждены. Регистрация Apple Developer и действующая Developer ID identity остаются внешним барьером. Смена пароля браузерного аккаунта пока не включает отдельный поток `vault.rewrap`; гарантированный durable retry для писем подтверждения также отложен. Для disaster recovery, RPO/RTO, HA и производительности отдельного подтверждения нет.

## FINAL_DETAILED_RELEASE_NOTES_EN

### New in 0.32.0

Selective Remote Cloud appears in a public release for the first time. Personal connections and workspace data can now be used on a Mac and in a browser through a client encrypted vault. Teams can share Hosts, Credentials, and Snippets in separate Team Vaults. The Mac app remains usable locally without an account.

### Personal Vault and sync

- The browser Personal Vault holds Hosts, Credentials, and Snippets encrypted on the client. Encrypted revisions sync with explicit conflict handling.
- macOS can enroll a Personal Vault, safely apply an existing download after sign in, and automatically sync supported resources in both directions.
- Personal Credentials and supported SSH key files sync as encrypted payloads; plaintext secrets are not published to Cloud.
- Browser storage is separated by account. On a trusted browser, an unlocked Vault can be restored after normal sign in and page reload without a second Vault password prompt.
- Missing key material and revision conflicts surface as recovery states instead of silently replacing data.

### Teams, access, and devices

- Team Vaults provide a separate shared space for Hosts, Credentials, and Snippets, with Owner, Admin, Member, and Viewer roles, invitations, and member management.
- Additional devices require admission. Clients encrypt and decrypt Team data; the server stores ciphertext and key wrappers for approved devices.
- Member or device removal and key rotation block unsafe writes until the required new wrappers are ready. Clients expose conflict, revocation, and missing wrapper states.
- Personal data and the Mac Keychain remain separate from Team Vault data. Team sessions do not silently borrow Personal credentials.
- Browser Team Vault metadata now interoperates with current macOS clients, and record updates preserve compatible fields.

### Hosts and connections

- Personal and Team Hosts now share consistent folders, search, sorting, list, and grid views. Nested folders, moving, and ordering stay within the appropriate data scope.
- A persistent Host Shelf offers quick access to selected Hosts; the catalog can collapse to leave more room for active work.
- Moving a Team Host between folders in the same Vault preserves newer Host settings after sync. Moving it across Vaults is blocked.
- Host, Target, and Vault pickers are searchable. Team Vault filters preserve user selection across relaunches.
- SSH password entry produces one visible prompt per connection attempt. Saved credentials are reused when suitable, and Cancel stops the affected attempt.
- Managed Jump Host credentials are isolated from destination credentials. The managed Jump Host transport was corrected; direct SSH and real managed Jump Host sessions were separately accepted before merge.
- Supported SSH/SFTP workflows also work with Team Hosts while respecting roles, Vaults, and the Personal/Team credential boundary.

### Credentials, Snippets, and workspaces

- Team Credentials and Team Snippets have their own shared libraries while Personal libraries stay in Personal Vault. Nested folders, search, and editing are more consistent across Personal and Team Snippets.
- Compact layouts improved across Hosts, Terminal, SFTP, Forwarding, and Tunnels. Actions remain available through compact toolbars and menus as windows narrow.
- Terminal active state is clearer in Light theme. RU/EN localization covers Cloud, settings, resource management, guidance, and affected macOS screens.
- What's New introduces Cloud as a new public capability. Team warnings better explain the affected scope and action.

### Reliability and security

- If Keychain credential deletion fails, deleting a Personal Host no longer reports success: the profile and metadata remain, and the error is shown.
- Credential inclusive encrypted backup export stops before writing an archive if legacy credential migration fails. Any previous archive remains intact.
- The updater verifies the pinned publisher before installation and rechecks the transactional copy before discarding its rollback backup.
- The official release path requires Developer ID Application, the matching Team ID, notarization, stapling, and Gatekeeper checks. These are publication gates, not claims that the 0.32.0 artifact already passed them.
- CI feedback was shortened by running Release compilation alongside the test job. This describes the build process, not app performance.

### Release limitations

An official signed and notarized 0.32.0 DMG, Gatekeeper acceptance, clean install, and upgrade acceptance remain unverified. Apple Developer enrollment and a valid Developer ID identity are external blockers. Browser password change does not yet include the separate `vault.rewrap` flow, and durable verification email retry is deferred. Disaster recovery, RPO/RTO, high availability, and performance claims have no separate acceptance evidence.

## SHORT_GITHUB_RELEASE_NOTES_RU

В 0.32.0 Selective Remote Cloud впервые входит в публичный релиз: Personal Vault со сквозным шифрованием синхронизирует Hosts, Credentials и Snippets между Mac и браузером. Team Vaults добавляют совместные ресурсы, роли, приглашения и контроль доступа устройств. Локальная работа без аккаунта сохраняется.

Появились постоянная Host Shelf, папки и перенос Hosts, поиск по Host/Target/Vault, более удобные Snippets и компактные раскладки Terminal/SFTP/Forwarding/Tunnels. SSH-пароли запрашиваются надёжнее, сохранённые учётные данные используются повторно, а Jump Host и конечный хост имеют раздельные секреты. Исправлены совместимость Team Vault metadata, удаление профилей при ошибке Keychain и миграция Credentials перед резервным копированием.

## SHORT_GITHUB_RELEASE_NOTES_EN

Selective Remote Cloud enters a public release for the first time in 0.32.0. An end-to-end encrypted Personal Vault syncs Hosts, Credentials, and Snippets between Mac and browser. Team Vaults add shared resources, roles, invitations, and device access controls. Local use without an account remains available.

A persistent Host Shelf, Host folders and moves, searchable Host/Target/Vault pickers, improved Snippets, and compact Terminal/SFTP/Forwarding/Tunnels layouts make daily work easier. SSH password prompts are more reliable, saved credentials are reused, and Jump Host secrets stay separate from destination secrets. This version also corrects Team Vault metadata compatibility, failure-safe profile deletion, and credential migration before backup.

## APP_WHATS_NEW_RU

- **Selective Remote Cloud** — впервые в публичной версии: работайте с подключениями на Mac и в браузере; локальный режим без аккаунта остаётся доступным.
- **Personal Vault** — Hosts, Credentials и Snippets шифруются на устройстве и синхронизируются между доверенными устройствами.
- **Team Vaults** — делитесь Hosts, Credentials и Snippets с командой через роли, приглашения и допуск устройств.
- **Быстрее к нужному Host** — постоянная Host Shelf, вложенные папки, поиск, сортировка и перенос Hosts в пределах своей области.
- **Надёжнее ежедневная работа** — улучшены запрос SSH-пароля, Jump Host, Snippets и компактные раскладки; исправлены ошибки Keychain и резервного копирования.

## APP_WHATS_NEW_EN

- **Selective Remote Cloud** — in a public release for the first time: work with connections on Mac and in the browser; local use without an account remains available.
- **Personal Vault** — Hosts, Credentials, and Snippets are encrypted on the device and synced across trusted devices.
- **Team Vaults** — share Hosts, Credentials, and Snippets with roles, invitations, and device admission.
- **Find Hosts faster** — use the persistent Host Shelf, nested folders, search, sorting, and scope aware Host moves.
- **More reliable daily work** — SSH password entry, Jump Host, Snippets, and compact layouts improved; Keychain deletion and backup migration failures are handled safely.
