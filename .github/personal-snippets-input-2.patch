+    const folders = showingSnippets ? ["none", ...paths.map((path) => `folder:${path}`)] : [...new Set(current.records.filter((record) => record.type === "host").map(hostFolderName))]
@@ -649 +792,3 @@ export async function initializeLocalVault({
-        option.textContent = value === "all" ? "Все папки" : value;
+        option.textContent = value === "all" ? "Все папки" : showingSnippets && value === "none" ? "Без папки"
+          : showingSnippets ? value.slice(7) : value;
+        if (value !== "all" && !(showingSnippets && value === "none")) option.setAttribute("translate", "no");
@@ -661,2 +806,2 @@ export async function initializeLocalVault({
-    const visibleRecords = sortLocalVaultRecords(filteredRecords.filter((record) => {
-      if (!query) return true;
+    let visibleRecords = sortLocalVaultRecords(filteredRecords.filter((record) => {
+      if (!query || showingSnippets) return true;
@@ -669,0 +815,7 @@ export async function initializeLocalVault({
+    if (showingSnippets) {
+      const original = new Map(visibleRecords.map((record) => [record.id, record]));
+      visibleRecords = filterTeamSnippets(visibleRecords.map(personalSnippetProjection), {
+        query, folder: folderFilter.value, sort: sort.value,
+      }).map((record) => original.get(record.id));
+      setText(snippetCount, `Сниппеты: ${visibleRecords.length} / ${snippets.length}`);
+    }
@@ -686,0 +839 @@ export async function initializeLocalVault({
+        : showingSnippets && snippets.length > 0 ? "Ничего не найдено. Измените поиск, папку или быстрый фильтр."
@@ -690,0 +844,7 @@ export async function initializeLocalVault({
+    const snippetTree = showingSnippets ? renderTeamSnippetTree({
+      documentValue, container: records, records: visibleRecords.map(personalSnippetProjection),
+      collapsed: activeSnippetCollapsedFolders(), editable: !conflictMode,
+      idPrefix: "personal-snippet-folder-content",
+      onToggle: (ids) => { visibleRecordIDs = ids; }, onCreateGroup: beginSnippetGroup,
+    }) : null;
+    if (snippetTree) visibleRecordIDs = snippetTree.visibleIDs;
@@ -814 +974,3 @@ export async function initializeLocalVault({
-      (folderContent ?? records).append(card);
+      const snippetContainer = snippetTree?.containers.get(record.id);
+      if (snippetContainer) snippetContainer.insertBefore(card, snippetContainer.querySelector(":scope > .snippet-folder-group"));
+      else (folderContent ?? records).append(card);
@@ -815,0 +978 @@ export async function initializeLocalVault({
+    for (const button of records.querySelectorAll(".record-actions button")) button.disabled = conflictMode;
@@ -819,0 +983,3 @@ export async function initializeLocalVault({
+    if (workspace.hidden || conflictMode || savePending) return;
+    const generation = scopeGeneration;
+    savePending = true;
@@ -824,0 +991 @@ export async function initializeLocalVault({
+      if (editingRecordID && (!existing || existing.type !== type.value)) throw new Error("record_changed");
@@ -830,0 +998,2 @@ export async function initializeLocalVault({
+        : type.value === "snippet"
+          ? personalSnippetRecordData({ title: title.value, body: secret.value, folder: snippetFolder.value, recordID: existing?.id }, existing?.data)
@@ -840,0 +1010 @@ export async function initializeLocalVault({
+      if (generation !== scopeGeneration) return;
@@ -847 +1017 @@ export async function initializeLocalVault({
-      setText(message, "Не удалось сохранить запись. Заполните обязательные поля.");
+      if (generation === scopeGeneration) setText(message, "Не удалось сохранить запись. Проверьте поля и путь папки; исходная запись сохранена.");
@@ -849 +1019,2 @@ export async function initializeLocalVault({
-      saveButton.disabled = false;
+      savePending = false;
+      saveButton.disabled = conflictMode;
@@ -852,0 +1024,38 @@ export async function initializeLocalVault({
+  documentValue.querySelector("#personal-snippet-create")?.addEventListener("click", beginCreate);
+  documentValue.querySelector("#personal-snippet-group-create")?.addEventListener("click", () => beginSnippetGroup());
+  snippetGroupName?.addEventListener("input", () => snippetGroupName.setCustomValidity(""));
+  snippetGroupParent?.addEventListener("change", () => snippetGroupName.setCustomValidity(""));
+  documentValue.querySelector("#personal-snippet-group-cancel")?.addEventListener("click", () => { snippetGroupForm.reset(); snippetGroupEditor.close(); });
+  snippetGroupForm?.addEventListener("submit", (event) => {
+    event.preventDefault();
+    if (workspace.hidden || conflictMode || savePending) return;
+    try {
+      const name = normalizeTeamSnippetFolder(snippetGroupName.value);
+      if (!name || name.includes("/")) throw new Error("invalid_snippet_folder");
+      const path = normalizeTeamSnippetFolder([snippetGroupParent.value, name].filter(Boolean).join("/"));
+      if (teamSnippetFolderPaths(controller.document().records.filter((record) => record.type === "snippet").map(personalSnippetProjection)).includes(path)) throw new Error("snippet_folder_exists");
+      snippetGroupEditor.close();
+      beginCreate();
+      type.value = "snippet";
+      snippetFolder.value = path;
+      updateLabels();
+      title.focus();
+    } catch {
+      const error = "Укажите новое имя без /; полный путь — не более 120 символов.";
+      snippetGroupName.setCustomValidity(documentValue.defaultView?.SelectiveRemoteI18n?.t(error) ?? error);
+      snippetGroupName.reportValidity();
+    }
+  });
+  for (const button of documentValue.querySelectorAll("[data-personal-snippet-expand]")) {
+    button.addEventListener("click", () => {
+      if (workspace.hidden) return;
+      const collapsed = activeSnippetCollapsedFolders();
+      collapsed.clear();
+      if (button.dataset.personalSnippetExpand === "false") {
+        const snippets = controller.document().records.filter((record) => record.type === "snippet");
+        for (const path of ["", ...teamSnippetFolderPaths(snippets.map(personalSnippetProjection))]) collapsed.add(path);
+      }
+      render();
+    });
+  }
+  editorDialog?.addEventListener("cancel", () => resetEditor());
@@ -864 +1073 @@ export async function initializeLocalVault({
-  search?.addEventListener("input", render);
+  search?.addEventListener("input", () => { snippetFilteredCollapsedFolders.clear(); render(); });
@@ -866 +1075 @@ export async function initializeLocalVault({
-  folderFilter?.addEventListener("change", render);
+  folderFilter?.addEventListener("change", () => { snippetFilteredCollapsedFolders.clear(); render(); });
@@ -869,0 +1079 @@ export async function initializeLocalVault({
+      resetSnippetBrowser();
@@ -883,0 +1094 @@ export async function initializeLocalVault({
+      snippetFilteredCollapsedFolders.clear();
@@ -947,0 +1159 @@ export async function initializeLocalVault({
+      resetSnippetBrowser();
@@ -950,0 +1163,5 @@ export async function initializeLocalVault({
+      if (value !== activeRecordFilter) {
+        resetSnippetBrowser();
+        activeSmartFilter = null;
+        for (const button of smartFilterButtons) button.classList.remove("active");
+      }
diff --git a/cloud/public/i18n.js b/cloud/public/i18n.js
index 8f46256..6dd59c0 100644
--- a/cloud/public/i18n.js
+++ b/cloud/public/i18n.js
@@ -17,0 +18,3 @@
+    "Команда и папка шифруются в Personal Vault и синхронизируются с приложением.": "The command and folder are encrypted in Personal Vault and synchronized with the app.",
+    "Далее откроется первый сниппет. Группа появится после его сохранения в Personal Vault.": "Next, create the first snippet. The folder appears after it is saved in Personal Vault.",
+    "Не удалось сохранить запись. Проверьте поля и путь папки; исходная запись сохранена.": "Could not save the record. Check its fields and folder path; the original record is unchanged.",
diff --git a/cloud/public/index.html b/cloud/public/index.html
index 55e3e04..bd477c3 100644
--- a/cloud/public/index.html
+++ b/cloud/public/index.html
@@ -13,2 +13,2 @@
-  <link rel="stylesheet" href="/styles.css?v=187">
-  <script src="/i18n.js?v=187"></script>
+  <link rel="stylesheet" href="/styles.css?v=188">
+  <script src="/i18n.js?v=188"></script>
@@ -431,0 +432,5 @@
+          <div class="snippet-browser-actions" id="personal-snippet-actions" hidden>
+            <p id="personal-snippet-count" role="status" aria-live="polite"></p>
+            <button type="button" id="personal-snippet-create">Добавить Snippet</button>
+            <button type="button" class="secondary" id="personal-snippet-group-create">Новая группа</button>
+          </div>
@@ -442 +447 @@
-          <label class="personal-vault-control" for="personal-vault-folder-filter"><span>Папка Hosts</span>
+          <label class="personal-vault-control" for="personal-vault-folder-filter" id="personal-vault-folder-control"><span id="personal-vault-folder-label">Папка Hosts</span>
@@ -445,0 +451,15 @@
+        <div class="snippet-tree-actions" id="personal-snippet-tree-actions" hidden>
+          <button type="button" class="secondary" data-personal-snippet-expand="true">Развернуть всё</button>
+          <button type="button" class="secondary" data-personal-snippet-expand="false">Свернуть всё</button>
+        </div>
+        <dialog class="record-editor-dialog" id="personal-snippet-group-editor" aria-labelledby="personal-snippet-group-title">
+          <div class="record-editor-heading"><h3 id="personal-snippet-group-title">Новая группа</h3></div>
+          <form class="record-form" id="personal-snippet-group-form">
+            <label for="personal-snippet-group-parent">Родительская папка</label>
+            <select id="personal-snippet-group-parent"><option value="">Без родительской папки</option></select>
+            <label for="personal-snippet-group-name">Название группы</label>
+            <input id="personal-snippet-group-name" maxlength="120" required autocomplete="off" aria-describedby="personal-snippet-group-help">
+            <p class="field-help snippet-group-help" id="personal-snippet-group-help">Далее откроется первый сниппет. Группа появится после его сохранения в Personal Vault.</p>
+            <div class="record-form-actions"><button type="submit">Продолжить</button><button type="button" class="secondary" id="personal-snippet-group-cancel">Отмена</button></div>
+          </form>
+        </dialog>
@@ -469,0 +490,6 @@
+          <label for="local-snippet-folder" id="local-snippet-folder-label" hidden>Папка сниппета</label>
+          <div class="field-with-help" id="local-snippet-folder-field" hidden>
+            <input id="local-snippet-folder" name="snippetFolder" maxlength="120" list="local-snippet-folder-options" placeholder="Например, Production/Deploy" aria-describedby="local-snippet-folder-help">
+            <datalist id="local-snippet-folder-options"></datalist>
+            <small class="field-help" id="local-snippet-folder-help">Выберите папку или задайте вложенный путь через /. Пустое поле — без папки.</small>
+          </div>
@@ -1027 +1053 @@
-  <script type="module" src="/app.js?v=187"></script>
+  <script type="module" src="/app.js?v=188"></script>
diff --git a/cloud/public/styles.css b/cloud/public/styles.css
index 148e7cf..2d1fe0f 100644
--- a/cloud/public/styles.css
+++ b/cloud/public/styles.css
@@ -1167,0 +1168,16 @@ button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-vis
+
+/* Personal snippet controls share the Team tree/card styles without sharing state. */
+#cloud-workspace #personal-snippet-actions { grid-column:1/-1; }
+#cloud-workspace #personal-snippet-tree-actions { margin:12px 0; }
+@media (max-width:560px) {
+  #cloud-workspace #personal-snippet-actions p { flex-basis:100%; }
+  #cloud-workspace #personal-snippet-actions button { flex:1; }
+}
+#cloud-workspace #personal-snippet-actions button,
+#cloud-workspace #personal-snippet-tree-actions button {
+  border:1px solid var(--line); border-radius:10px; padding:10px 14px;
+  background:transparent; color:var(--ink); font:inherit; font-size:13px; font-weight:750; cursor:pointer;
+}
+#cloud-workspace #personal-snippet-actions #personal-snippet-create { border-color:transparent; background:var(--accent); color:#082016; }
+#cloud-workspace #personal-snippet-tree-actions button { padding:6px 10px; font-size:11px; }
+#cloud-workspace #personal-snippet-actions button:disabled { opacity:.55; cursor:not-allowed; }
diff --git a/cloud/public/team-snippet-browser.js b/cloud/public/team-snippet-browser.js
index c7bbb20..6f20126 100644
--- a/cloud/public/team-snippet-browser.js
+++ b/cloud/public/team-snippet-browser.js
@@ -95 +95 @@ export function visibleTeamSnippetIDs(tree, collapsed) {
-export function renderTeamSnippetTree({ documentValue, container, records, collapsed, editable, onToggle, onCreateGroup }) {
+export function renderTeamSnippetTree({ documentValue, container, records, collapsed, editable, onToggle, onCreateGroup, idPrefix = "team-snippet-folder-content" }) {
@@ -117 +117 @@ export function renderTeamSnippetTree({ documentValue, container, records, colla
-    content.id = `team-snippet-folder-content-${++sequence}`;
+    content.id = `${idPrefix}-${++sequence}`;
diff --git a/cloud/tests/public-i18n.test.mjs b/cloud/tests/public-i18n.test.mjs
index 4be0594..8e35178 100644
--- a/cloud/tests/public-i18n.test.mjs
+++ b/cloud/tests/public-i18n.test.mjs
@@ -10 +10 @@ test("public portal exposes a persistent RU and EN locale switch", () => {
-  assert.match(html, /\/i18n\.js\?v=187/);
+  assert.match(html, /\/i18n\.js\?v=188/);
diff --git a/cloud/tests/public-vault-ui.test.mjs b/cloud/tests/public-vault-ui.test.mjs
index e566349..49f648a 100644
--- a/cloud/tests/public-vault-ui.test.mjs
+++ b/cloud/tests/public-vault-ui.test.mjs
@@ -502,2 +502,2 @@ test("portal exposes separate public, authentication and workspace states", asyn
-  assert.match(html, /<script src="\/appearance-bootstrap\.js\?v=162"><\/script>\s*<link rel="stylesheet" href="\/styles\.css\?v=187">/u);
-  assert.match(html, /\/app\.js\?v=187/u);
+  assert.match(html, /<script src="\/appearance-bootstrap\.js\?v=162"><\/script>\s*<link rel="stylesheet" href="\/styles\.css\?v=188">/u);
+  assert.match(html, /\/app\.js\?v=188/u);
@@ -673,2 +673,2 @@ test("portal exposes separate public, authentication and workspace states", asyn
-  assert.match(html, /styles\.css\?v=187/u);
-  assert.match(html, /app\.js\?v=187/u);
+  assert.match(html, /styles\.css\?v=188/u);
+  assert.match(html, /app\.js\?v=188/u);
@@ -859,2 +859,2 @@ test("portal exposes separate public, authentication and workspace states", asyn
-  assert.match(html, /app\.js\?v=187/u);
-  assert.match(html, /styles\.css\?v=187/u);
+  assert.match(html, /app\.js\?v=188/u);
+  assert.match(html, /styles\.css\?v=188/u);
