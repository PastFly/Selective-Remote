# v0.27.0 architecture note

The terminal UX changes reuse `TerminalCommandHistoryStore`, `TerminalWorkspaceInspector`, and the existing terminal session runtime. Ordered Startup Snippets are represented by a hidden multiline composite template only when more than one snippet is selected, preserving the legacy single-snippet runtime and profile format.
