## 0.21.14

- Небольшие улучшения встроенной справки и системного меню приложения.
- Рабочая логика RDP, SSH, Terminal, SFTP и Forwarding в этом релизе не изменялась.

## 0.21.13

- Полностью обновлён интерфейс настройки SSH-профиля в разделе «Подключения»: добавлены современная шапка, вертикальная навигация, быстрые сведения и адаптивная сводка маршрута Mac → Jump Host → SSH-сервер.
- Настройки SSH разделены на «Основное», «Аутентификация», «Маршрут» и «Безопасность», а рабочие области Terminal, SFTP и «Туннели» сохранили полноразмерную компоновку без тесной боковой сводки.
- Focus mode терминала получил отдельную постоянно видимую кнопку «Вернуть интерфейс» вместо действия, скрытого в меню «…».
- Компоновку Terminal Grid теперь можно выбрать и сохранить даже без открытых терминалов; пустая сетка сразу показывает четыре SSH-ячейки с понятным действием добавления панели.
- Исправлена верхняя часть sidebar при доступном обновлении и активных сессиях/туннелях: название Selective Remote и уведомление больше не сжимаются в узкие вертикальные столбцы.
- Добавлены regression tests современного SSH UI, независимых рабочих областей, пустой сетки, Focus mode и устойчивой компоновки уведомления об обновлении.
- RDP fullscreen/Retina/multi-monitor/topology, системный SSH/PTY, SFTP service и логика запуска Forwarding в этом релизе не изменялись.

## 0.21.12

- Выполнена безопасная очистка репозитория без изменения пользовательского поведения приложения.
- Удалён неиспользуемый старый интерфейс независимых SSH-туннелей; глобальный раздел Forwarding продолжает работать через актуальный Forwarding Manager.
- Regression tests туннелей перенаправлены на действующий Forwarding Manager с сохранением проверок Profile/Independent и запуска двойным щелчком.
- Удалены устаревшие временные материалы: аварийный Retina patcher, связанный README и две неиспользуемые старые версии скриншотов.
- RDP fullscreen/Retina/multi-monitor/topology, SFTP runtime и действующий Forwarding runtime в этом релизе не изменялись.

## 0.21.11

- Полностью обновлён интерфейс настройки RDP-профиля в разделе «Подключения»: добавлены современная шапка профиля, вертикальная навигация по разделам, быстрые сведения и адаптивная сводка подключения.
- Все существующие рабочие RDP-настройки продолжают использовать прежнюю логику: дисплеи, RD Gateway, устройства, папки, безопасность и нижняя панель подключения не переписывались.
- В RDP-профиль добавлено сохраняемое поле «Описание» для назначения или заметки о подключении.
- Сохранена обратная совместимость: существующие профили без поля описания открываются с пустым значением и не требуют миграции.
- Экспорт и импорт профилей Selective Remote сохраняет новое поле описания.
- SSH-профили продолжают использовать прежний интерфейс.
- Добавлены regression tests для совместимости старых профилей и Codable/export-import round trip поля описания.
- RDP fullscreen/Retina/multi-monitor/topology runtime, SFTP и Forwarding в этом релизе не изменялись.

## 0.21.10

- Исправлена диагностика разрешений камеры и микрофона: System Check различает состояние основного приложения и последние известные privacy-маркеры реальных RDP Session, не читает TCC.db и не вызывает скрытые запросы разрешений.
- «Проверка системы» получила сводку готовности Selective Remote, статусы SSH / RDP / камеры / микрофона / обновлений, фильтр «Только проблемы» и безопасные действия без автоматического исправления системы.
- После реального обновления выполняется одноразовая тихая проверка критичных packaged-компонентов и update feed; она не запускает SSH/RDP, Touch ID или запросы Camera/Microphone и показывает предупреждение только при действительно важной проблеме.
- Карточка версии в Diagnostics теперь открывает встроенную историю изменений, а окно «Что нового» показывает также внутренний build установленной версии.
- Connection Center получил единый «Сбросить» для поиска, фильтров, сортировки и настройки колонок.
- Убран deprecated-вызов активации NSRunningApplication, использовавшийся в macOS 14.
- Исправлена совместимость нового Diagnostics System Check со Swift 6 в GitHub Actions.
- README.md и README_EN.md обновлены актуальными скриншотами Connection Center и Forwarding Manager.
- RDP fullscreen/Retina/topology, SFTP runtime и Forwarding runtime в этом релизе не изменялись.

## 0.21.9

- Добавлен постоянный пункт `Справка → Что нового…`: историю изменений теперь можно открыть в любой момент, а после обновления новая версия показывается один раз автоматически.
- Update Experience 2.0 получил более понятные этапы обновления, время последней успешной проверки и локальную историю изменений внутри приложения для работы без доступа к GitHub.
- Connection Center получил кликабельные summary-карточки, отдельный фильтр по состоянию, сохранение фильтров и сортировки, настройку видимости колонок и дополнительные контекстные действия.
- Terminal Workspace получил поиск по выводу через `⌘F`, навигацию по найденным совпадениям, быстрые переключения вкладок с клавиатуры и переименование вкладки двойным щелчком.
- «Связка ключей» получила поиск и сортировку для SSH ID, сохранённых SSH-паролей, Known Hosts и SSH CA без изменения существующего Inspector.
- Diagnostics Center получил раздел «Проверка системы» с безопасными preflight-проверками приложения, SSH, Keychain, Touch ID, разрешений macOS, FreeRDP helpers и update feed без запуска реальных RDP/SSH-сессий и без чтения секретов.
- Сохранена совместимость с macOS 14.0 для настраиваемых колонок Connection Center; исправлена совместимость Keychain search/sort с Swift compiler в GitHub Actions.
- Добавлены regression tests для Update Experience, Connection Center, Terminal, Keychain и Diagnostics; функциональная сборка 0.21.9 прошла полный CI.

## 0.21.8

- Кнопка «Что нового» теперь открывает нативное окно Selective Remote вместо перехода на GitHub.
- История изменений показывает все пропущенные версии между установленной и доступной версией — от самой свежей к более старым.
- Для истории добавлены состояния загрузки, повторной попытки и ошибки; окно остаётся читаемым при пропуске нескольких релизов.
- Добавлена отдельная английская история `CHANGELOG_EN.md`; приложение выбирает RU/EN источник по языку интерфейса и использует резервный язык, если нужный источник временно недоступен.
- Update manifest расширен отдельными RU/EN URL истории изменений при сохранении `releaseNotesURL` для обратной совместимости со старыми версиями приложения.
- Добавлены regression tests для фильтрации пропущенных версий, порядка релизов и декодирования локализованных URL истории.

## 0.21.7

- Доведена до конца настройка размера текста интерфейса: Small / Normal / Large / Very Large теперь используют четыре явно различимых native point size без масштабирования всего окна и без вмешательства в Retina/DPI.
- Улучшена «Связка ключей»: системный контраст иконок для выбранной строки, более последовательная русская локализация SSH ID, сертификатов, паролей и Known Hosts.
- Доработан Diagnostics Center: локализованы пользовательские подписи и состояния, ограничена максимальная ширина содержимого на широких окнах, при этом технический Raw Report сохранён отдельным диагностическим форматом.
- README.md и README_EN.md синхронизированы и переписаны как актуальное описание продукта вместо набора исторических заметок по релизам; текущие возможности RDP, Terminal Workspace, SFTP, Forwarding, Connection Center, Diagnostics и Keychain собраны в единую структуру.
- Добавлены regression tests для размеров текста, сохранения настройки, selected-state контраста, Diagnostics Center и актуальной структуры английской документации.

## 0.21.6

- Исправлен полноэкранный RDP на Retina и в multi-monitor конфигурациях macOS: устранено отображение удалённого рабочего стола в части экрана, сохранён корректный 1× SDL framebuffer и восстановлен настоящий fullscreen.
- Исправлена геометрия встроенного дисплея MacBook: Windows taskbar остаётся видимой, а штатная верхняя панель macOS снова доступна при подводе указателя к верхнему краю.
- Исправлена ручная раскладка RDP-мониторов: экраны точно привязываются к соседним границам, а сохранённые микрозазоры автоматически нормализуются перед запуском FreeRDP.
- Настройка размера текста теперь действительно влияет на интерфейс macOS; улучшен контраст выделенных элементов в «Связке ключей».
- Доработан Diagnostics Center: улучшены русская локализация, адаптивная компоновка и отображение runtime-состояний на широких окнах.
- README упрощён: подробная история старых 0.21.x релизов перенесена из верхней части документа в CHANGELOG.

## 0.21.5

- Обновлён механизм обновлений: проверка при запуске и примерно каждые 5 часов, индикатор новой версии, ненавязчивые системные уведомления и опциональная автоматическая загрузка.
- Добавлена подтверждаемая пользователем установка DMG с прогрессом, SHA-256/codesign/bundle-id проверками, безопасным откатом и перезапуском без sudo; если каталог приложения недоступен для записи, используется ручной сценарий через Finder.
- Проведён аудит RU/EN локализации ключевых пользовательских разделов и динамических статусов.
- Добавлены native-настройки внешнего вида: System/Light/Dark, размер текста и плотность интерфейса; сохранены и упорядочены темы терминала.
- Diagnostics Center обновлён до структурированного интерфейса с поиском, секциями и Raw Report при сохранении безопасного Copy/Export отчёта.
- Схема маршрута Forwarding стала адаптивной: на узкой панели автоматически используется вертикальная компоновка без обязательного горизонтального скролла.
- Добавлены тесты политики уведомлений об обновлениях.

## 0.21.4

- Улучшена схема Forwarding: для узлов маршрута увеличена ширина и добавлено масштабирование текста, поэтому IPv4/IPv6 endpoint и порт больше не должны обрезаться.
- Destination у Local tunnel теперь может перейти в подтверждённое зелёное состояние по реальным событиям OpenSSH: после фактического обращения через локальный порт и при отсутствии последующего `connect failed` в журнале.
- Ошибка открытия конечного TCP-канала (`open failed: connect failed`) отображается на Destination как ошибка, а до первого реального обращения конечный узел остаётся неизвестным.
- Журнал SSH-туннеля переведён на `LogLevel=DEBUG1`, чтобы видеть создание listener, реальные попытки forwarding и ошибки открытия канала.
- В начало журнала добавлена безопасная runtime-сводка: mode, ownership, bind, destination, SSH endpoint, auth type, Jump Host/Proxy, KeepAlive и последняя ошибка — без password/passphrase.
- В диагностике Forwarding добавлены подтверждение local listener и состояние Destination по журналу OpenSSH.
- Добавлены regression tests для runtime evidence Local forwarding и подробного DEBUG1 logging.

## 0.21.3

- Исправлено нестабильное выделение туннелей в Forwarding Manager: одиночный клик снова надёжно выбирает Profile и Independent tunnel, при этом запуск остановленного туннеля двойным кликом сохранён.
- Исправлен бесконечный focus-loop панели «История и подсказки» при переключении между активными Terminal panes в split/grid-компоновке.
- Программное скрытие истории у предыдущей Terminal pane больше не возвращает ей focus; ручное закрытие истории по-прежнему возвращает focus текущему терминалу.
- Сохранены контекстные действия Forwarding Manager и исправления Terminal Grid из 0.21.2.
- Добавлены regression tests для выбора туннелей и переключения Terminal panes с открытой историей.

## 0.21.2

- Connection Center получил сортировку по столбцам и контекстные действия для runtime-подключений.
- В Forwarding Manager восстановлен запуск остановленного туннеля двойным кликом и добавлено контекстное меню действий для Profile и Independent tunnels.
- Ручное отключение Terminal больше не отображается как ошибка `Exit code 255`; пользовательский Disconnect остаётся нормальным состоянием `Disconnected` и не запускает Smart Reconnect.
- Исправлена гонка панели «История и подсказки» при переключении между активными terminal panes в split/grid-компоновке.
- Исправлено обрезание подписей верхних summary-карточек в Connection Center и Forwarding Manager.
- Кнопка подключения в существующей Terminal pane теперь повторно подключает к тому же серверу; выбор нового сервера остаётся отдельным действием.
- В свободной ячейке Terminal Grid добавлена кнопка `+` для быстрого создания новой SSH-панели.
- Server Commands 2.0 получил фильтр `Все / Активные / Неактивные`, сортировку активных служб и выровненный поиск в header.
- Добавлены regression tests для новых Terminal/Grid, Connection Center, Forwarding и Server Commands сценариев.

## 0.21.1

- Добавлен Connection Center — единый центр реальных runtime-подключений RDP, Terminal, SFTP, Profile Forwarding и Independent Forwarding с состояниями, uptime, инспектором и действиями.
- Forwarding переработан в единый Forwarding Manager 2.0 с раздельным ownership Profile/Independent, инспектором, логами и наглядной схемой Local / Remote / Dynamic с Jump Host и proxy там, где они реально используются.
- Terminal Workspace 2.0 получил более заметные active tab/pane, runtime-состояния и uptime, быстрый reconnect, duplicate-with-connect и усиленную индикацию Broadcast Input.
- Добавлен Smart Reconnect с ограниченными попытками и backoff для временных сетевых сбоев; ручное отключение не запускает автоматическое переподключение, а Touch ID не вызывается циклически.
- Server Commands 2.0 теперь формирует команды на основе реального remote discovery: systemd, журналы, сеть, диски, Docker/Podman и другие доступные инструменты, используя существующую Terminal/SSH-сессию.
- Quick Connect 2.0 понимает user@host, user@host:port и ssh-команды, поддерживает recent targets, auth mode, SSH ID, Touch ID Key, ssh-agent, Jump Host и Save as Profile.
- Добавлен безопасный Diagnostics Center 2.0 с Copy Diagnostic и Export Diagnostic для runtime-состояния приложения; пароли, passphrase, Keychain secrets и proxy secrets в отчёт не попадают.
- Обновлены README.md и README_EN.md; добавлены анонимизированные визуальные превью Connection Center и Forwarding Manager 2.0.
- Расширены regression tests для новых runtime-сценариев, reconnect, Server Commands, Quick Connect и безопасной диагностики.

## 0.20.18

- Исправлен запуск SSH-туннелей двойным кликом в разделе Forwarding.
- Исправлен двойной клик для независимых туннелей и туннелей конкретного SSH-профиля.
- Одинарный клик по-прежнему только выделяет туннель.
- Двойной клик по выключенному туннелю запускает его без нарушения состояния выделения.

## 0.20.17

- Исправлены повторные запросы Touch ID после подключения SSH в Terminal Workspace.
- Служебная загрузка «Команд сервера» больше не требует повторного подтверждения уже авторизованной активной SSH-сессии.
- Server context сохраняется при переходе между разделами приложения и повторно используется при возвращении в Terminal.
- При реальном reconnect Touch ID запрашивается заново один раз, без дополнительного запроса для server context.
- Авторизация остаётся изолированной между разными Terminal-вкладками и SSH-подключениями.
- «История и подсказки» вынесена в отдельную кнопку панели Terminal.

## 0.20.16

- Исправлено получение контекста сервера для активной вкладки и панели Terminal Workspace.
- Служебный SSH-запрос server context теперь использует настройки аутентификации текущего Terminal-подключения, включая Keychain, SSH ID, ssh-agent, Jump Host, proxy и OpenSSH certificate.
- Исправлено появление ошибки авторизации Jump Host при успешно работающей основной SSH-сессии.
- Устаревший server context больше не сохраняется после смены подключения или reconnect.
- Добавлено автоматическое обновление контекста при reconnect и переключении активной terminal pane.
- Добавлена кнопка «Повторить» при ошибке получения сведений о сервере.
- Добавлены regression tests для привязки server context к конкретной Terminal-вкладке.

## 0.20.15

- Исправлено нестабильное выделение туннелей в глобальном Forwarding после запуска и остановки.
- Исправлен повторный выбор уже активного SSH-профиля после перехода в другой глобальный раздел.
- Jump Host теперь использует сохранённый пароль bastion-профиля из macOS Keychain.
- Добавлено раздельное безопасное AskPass-маршрутизирование для target SSH и Jump Host.
- Поддержка Jump Host с сохранённым паролем добавлена для Terminal, SFTP, Forwarding и установки public key.
- Полностью переработана вкладка «Туннели» внутри SSH-профиля.
- Профильные туннели получили двухзонный интерфейс со списком и Inspector.
- Добавлены визуальные схемы Local, Remote и Dynamic для профильных туннелей.
- Добавлены двойной клик, запуск, остановка, перезапуск, журнал и удаление профильных туннелей.
- Добавлена визуальная индикация и анимация активного профильного туннеля.
- Разделены состояния профильных и независимых SSH-туннелей.
- Глобальный Forwarding теперь показывает и считает только независимые туннели.

## 0.20.14

- Переработан Terminal Workspace: улучшена верхняя панель, добавлены pin вкладок, цветовые метки, reconnect и более заметный режим синхронного ввода.
- Добавлен глобальный Quick Connect по Cmd+K для быстрого запуска Terminal, SFTP и RDP.
- Добавлен импорт SSH-хостов из ~/.ssh/config.
- Добавлен визуальный Jump Host / ProxyJump UX для SSH-профилей.
- Добавлена поддержка использования сохранённых SSH-профилей как bastion/jump host.
- Расширен Keychain: добавлен раздел SSH Certificate Authorities.
- Добавлен импорт SSH CA public key.
- Добавлен выпуск OpenSSH certificates через существующий системный ssh-keygen.
- Улучшено управление сертификатами и отображение срока их действия.
- Исправлено отображение фильтров Keychain: все разделы теперь помещаются без скрытой горизонтальной прокрутки.
- Добавлена встроенная справка Selective Remote.
- Обновлены README.md и README_EN.md.
- Улучшены контекстные действия и группировка SSH-профилей.
- Исправлены Swift 6 actor-isolation и type-check проблемы в Keychain 4.0.

## 0.20.13

- Добавлен раздел Known Hosts в глобальный Keychain.
- Добавлен просмотр SSH host key, fingerprint, типа ключа и связанных профилей.
- Добавлена проверка текущего host key сервера через системный OpenSSH.
- Selective Remote предупреждает, если сохранённый SSH host key изменился.
- Добавлено безопасное удаление known_hosts-записей с резервной копией файла.
- Добавлен drag-and-drop импорт SSH ID в Keychain.
- Улучшено отображение срока действия OpenSSH certificates.
- В Terminal Workspace добавлено создание копии вкладки из контекстного меню.
- Расширены тесты для Known Hosts и Terminal Workspace.

## 0.20.12

- Добавлены контекстные действия для SSH Forwarding, Keychain, Terminal и хостов в sidebar.
- Туннели Forwarding теперь можно запускать двойным кликом.
- Для туннелей добавлены действия запуска, остановки, перезапуска, копирования, удаления и просмотра журнала.
- Кнопка создания нового туннеля перенесена над списком существующих туннелей.
- Работающий туннель получил визуальную индикацию и анимацию схемы.
- В Keychain добавлены контекстные действия для SSH ID и сохранённых credentials.
- В Terminal расширены действия контекстного меню вкладок.
- В sidebar добавлены контекстные действия для RDP и SSH-хостов.
- Улучшены окна подключения к произвольному SSH/SFTP-серверу.
- Для ручного подключения добавлено поле пароля и возможность сохранить сервер как SSH-профиль.
- Если логин ручного SSH-подключения не указан, по умолчанию используется root.
- Исправлена безопасная очистка временного SSH-пароля.
- Обновлён тест независимых SSH-подключений терминальных вкладок.

## 0.20.11

- Переработан глобальный Keychain: добавлен трёхзонный интерфейс со списком credentials и отдельным Inspector выбранного элемента.
- Добавлены подробности SSH-ключей: алгоритм, fingerprint, путь, public key, связанные профили и состояние ssh-agent.
- Touch ID Key теперь является отдельным типом credential и использует только обычные ECDSA-ключи.
- Добавлена проверка несовместимых ключей для Touch ID на уровне SSH-аутентификации.
- Добавлено автоматическое обнаружение и отображение OpenSSH certificates (*-cert.pub).
- OpenSSH certificates теперь передаются в SSH через CertificateFile.
- Улучшено создание SSH ID и Touch ID Key.
- Переработан глобальный Forwarding: список туннелей и отдельный Inspector.
- Добавлены визуальные схемы для Local, Remote и Dynamic SSH forwarding.
- Сохранены существующие функции запуска, остановки, Keychain, Touch ID и диагностики туннелей.

## 0.20.10

- Добавлен отдельный глобальный раздел Keychain для управления SSH ID, сохранёнными SSH credentials, Touch ID и ssh-agent.
- Credential Manager показывает тип и fingerprint SSH-ключа, путь к приватному ключу и связанные SSH-профили; приватные ключи по-прежнему остаются файлами и не копируются в Keychain.
- В SSH Diagnostics добавлена явная кнопка «Готово», сохранено закрытие через Esc и добавлена поддержка Cmd+W.
- Кнопка «Проверить снова» обновляет результаты диагностики, не закрывая окно.
- Конфигурация OpenSSH в диагностике теперь показывает краткое понятное описание, а полные аргументы доступны через раскрываемый блок «Показать параметры».
- Исправлена компоновка встроенного SFTP: двухпанельный файловый менеджер теперь использует всю доступную высоту рабочей области и динамически меняет размер вместе с окном.

## 0.20.9

- Added SSH diagnostics for TCP/proxy/authentication configuration.
- Added secure HTTP CONNECT and SOCKS5 proxy username/password authentication with proxy passwords stored in macOS Keychain.
- Migrated Keychain operation prompts to `LAContext` authentication contexts.
- Refined SSH credentials and proxy UI.

## 0.20.8

- Исправлена геометрия xterm в разделённых и сеточных терминалах: внутренний отступ больше не завышает доступную высоту FitAddon, поэтому последняя строка каждой панели полностью помещается.
- Русский и английский README полностью актуализированы под текущие SSH credentials, Touch ID Key, SFTP, независимые терминалы, forwarding и proxy.

## 0.20.7

- Исправлено восстановление полноэкранных TUI-программ (`nano`, `vim`, `tmux`) после переключения между SSH-хостами.
- Терминал теперь выполняет реальный PTY resize-pulse, чтобы удалённая программа получила `SIGWINCH` и полностью перерисовала экран.
- Повторный fit выполняется также при повторном появлении WebView в окне.

## 0.20.6

- Исправлено восстановление полноэкранных TUI-приложений (`nano`, `vim`, `tmux`) после переключения между SSH-хостами: терминал принудительно запрашивает перерисовку через PTY resize.
- Для каждого хоста запоминается последняя открытая вкладка (`Основные`, `Терминал`, `SFTP`, `Туннели`, `Безопасность`) и восстанавливается при возврате к хосту.
- Подсказки терминала получили нечёткий поиск по частям пути: `nano .ssh` теперь находит `nano ~/.ssh/authorized_keys`; каталог команд расширен SSH, systemd и диагностическими командами.
- Установка Touch ID/SSH-ключа больше не использует операцию, способную быть неоднозначной для пользователя: ключ добавляется идемпотентно через `grep` + `>>`, существующий `~/.ssh/authorized_keys` никогда не перезаписывается.

- Исправлено масштабирование и восстановление встроенного терминала при переключении SSH-профилей.
- Счётчик в разделе «Терминал» теперь показывает только независимые терминалы этого раздела.
- Установка публичного SSH-ключа использует сохранённый SSH-пароль из Keychain, когда он доступен.
- Touch ID больше не запрашивается для режима ssh-agent и не применяется к обычному ключу, если защита ключа не включена.
- Явный режим входа больше не переиспользует ControlMaster, созданный другим способом аутентификации.
- Добавлены подсказки для ~/.ssh/authorized_keys.
- HTTP CONNECT proxy получил поддержку имени пользователя для proxy-аутентификации.
- Окно SSH ID и секретов упрощено и снабжено пояснениями по ssh-agent, Keychain и Touch ID.


# История изменений Selective Remote

[← Вернуться на главную страницу](README.md)

## 0.20.4

- SSH authentication: добавлен явный выбор способа входа — автоматический, пароль, SSH-ключ, Touch ID Key или системный ssh-agent / ~/.ssh/config.
- Touch ID Key: добавлен отдельный сценарий создания ECDSA P-256 ключа с обязательным Touch ID перед его использованием и установкой обычного публичного ключа в authorized_keys сервера.
- OpenSSH теперь строго соблюдает выбранный режим: в режиме ключа пароль не используется как fallback, а в режиме пароля отключена public-key аутентификация.
- Карточка SSH credentials переработана в более компактный интерфейс в стиле host credentials: отображаются только параметры выбранного способа входа.
- Proxy: для SSH-профиля добавлены HTTP CONNECT и SOCKS5 proxy без отдельной proxy-аутентификации; настройка автоматически применяется к Terminal, SFTP и Forwarding.

## 0.20.3

- Исправлено восстановление SSH-пароля после недоступной legacy-записи Keychain: SSH/Forwarding используют новый namespace, поэтому старая ACL больше не блокирует повторное сохранение.
- Touch ID теперь проверяется через LocalAuthentication непосредственно перед чтением SSH-пароля и использованием защищённого SSH-ключа.
- Для Touch ID больше не создаются biometric ACL-записи Keychain, которые в ad-hoc GitHub-сборках могли завершаться `errSecMissingEntitlement (-34018)`.
- Отмена Touch ID не передаёт SSH-пароль и останавливает запуск подключения.

## 0.20.2

- Keychain recovery: stale or incorrectly approved SSH password records can be reset from the profile or forwarding card without touching other credentials.
- Touch ID: saved SSH passwords now use `biometryCurrentSet`, so protected access requires the enrolled Touch ID set instead of falling back to the Mac login password.
- AskPass: the helper no longer reads Selective Remote Keychain records directly. The main app unlocks the SSH password (including Touch ID) and passes it through a short-lived `0600` handoff file, eliminating the extra legacy “Always Allow” Keychain dialog caused by the separately signed helper.
- Credentials window: removed the misleading “Open system storage” button and clarified that SSH passwords/passphrases live in Keychain while private SSH keys remain files in `~/.ssh`.
- Forwarding: manual SSH-password records get the same Touch ID-only protection and per-tunnel Keychain repair action.

## 0.20.1

- SSH credentials: optional Touch ID / macOS user-presence protection for saved SSH passwords in Keychain.
- SSH ID: redesigned credentials UI, Ed25519 key generation flow and one-click public-key installation on the selected server.
- SSH keys: optional Touch ID gate before Selective Remote uses the selected private key.
- Forwarding: manual SSH passwords can be protected with Touch ID; saved profiles inherit their SSH credential security settings.

## 0.20.0

- SSH-профили получили отдельный пароль сервера, который хранится только в macOS Keychain и не попадает в экспорт профилей;
- Terminal, SFTP и SSH forwarding умеют использовать сохранённый пароль профиля через защищённый AskPass helper без передачи секрета в аргументах процесса;
- независимый forwarding для ручного SSH-сервера получил собственное поле пароля с сохранением и удалением записи Keychain;
- фоновые туннели поддерживают password/keyboard-interactive аутентификацию, а не только SSH-ключ и ssh-agent.

## 0.19.9

- Fixed SFTP upload progress totals for directories containing hidden files or hidden subdirectories. The calculated total now matches the tree that OpenSSH SFTP actually transfers recursively.

## 0.19.8

- Исправлено надёжное выделение строк SFTP/локальной панели с поддержкой Cmd и Shift.
- Для больших файлов и папок используется линейный индикатор прогресса; для каталогов считается общий и переданный объём.
- Добавлено тихое периодическое обновление удалённой панели: чаще во время активной передачи и реже в простое.

## 0.19.7

- Исправлена сборка SFTP-подсказок на CI: вместо несуществующего `localizedCaseInsensitiveHasPrefix` используется совместимый case-insensitive prefix search; также устранены предупреждения `unused` в тексте подтверждения удаления.
- Передачи больших одиночных файлов: общий размер теперь берётся из уже загруженной локальной строки и дополнительно определяется с security-scoped доступом; прогресс удалённого файла проверяется через быстрый `wc -c` по существующему SSH master с SFTP-fallback. Благодаря этому для большого файла отображается процентная полоса, объём и скорость, а не только «Передаётся».
- SFTP: размеры обычных удалённых каталогов рассчитываются в фоне через `du -sk` одним SSH-процессом и подставляются в колонку «Размер». Для виртуальных/смонтированных системных каталогов в корне (`/proc`, `/sys`, `/dev`, `/run`, `/mnt`, `/media`) автоматический обход намеренно не выполняется, чтобы не нагружать сервер.

## 0.19.6

- SFTP: исправлен ложный отказ при первом подключении по паролю — успешный bootstrap `ControlPersist` больше не трактуется как ошибка с кодом 0, если управляющий сокет уже поднят.
- SFTP: убран конфликт жеста двойного клика с системным выделением `List`; одиночное выделение снова работает штатно, двойной клик открывает объект.
- SFTP: добавлено множественное выделение файлов и папок (`⌘`/`Shift`) с пакетной загрузкой, скачиванием и удалением.
- SFTP: удаление каталогов ускорено — на обычном SSH-сервере дерево удаляется одной командой через уже открытый master-канал; для серверов только с `internal-sftp` оставлен безопасный рекурсивный fallback.
- SFTP: в поле пути добавлены подсказки переходов по дочерним папкам, родительскому каталогу, домашней папке и корню.

## 0.19.5

- Исправлена ошибка Swift 6 CI в `SFTPRightClickSelectionView.deinit`: монитор ПКМ теперь снимается при отвязке view от окна без вызова MainActor-изолированного метода из `deinit`.
- Удаление удалённой папки стало рекурсивным: сначала удаляется содержимое, затем сама папка.
- Диалог удаления явно предупреждает, что папка и всё её содержимое будут удалены безвозвратно.

## 0.19.4

- SFTP: правый клик теперь сразу выделяет строку до открытия контекстного меню — одинаково для локальной и удалённой панели.
- SFTP: двойной левый клик больше не имеет high-priority gesture, который задерживал обычное выделение строки.
- Передачи: сетевой/файловый замер прогресса вынесен с MainActor, поэтому долгий SFTP probe больше не подвешивает интерфейс.
- Передачи каталогов отображаются как неопределённый прогресс вместо ложного 0% до самого завершения.

## 0.19.3

- SFTP теперь создаёт одно отдельное управляющее SSH-соединение перед первой файловой операцией; пароль или passphrase запрашивается один раз, а последующие чтение каталогов, загрузки, переименования и удаления работают через готовый `ControlPath`;
- дочерние процессы SFTP запускаются с `BatchMode=yes`, поэтому при потере master-соединения они не открывают повторные окна пароля;
- исправлён конфликт жестов в строках файлового менеджера: одиночный клик оставлен системному выбору строки, а двойной левый клик получает высокий приоритет и открывает папку/файл; правая кнопка по-прежнему открывает контекстное меню.

## 0.19.2

- создание DMG теперь сохраняет device node, возвращённый `hdiutil attach`, и
  отключает образ по стабильному `/dev/disk…`, а не по исчезающему пути тома;
- финальная очистка проверяет `hdiutil info`: уже отключённый образ считается
  успешно очищенным, а действительно занятый образ по-прежнему останавливает
  публикацию.

## 0.19.1

- исправлено подключение SFTP к серверу с парольной аутентификацией: пакетный
  режим OpenSSH больше не подавляет защищённый запрос пароля;
- в защищённом окне пароля и passphrase теперь работают `⌘V`, пункт
  **Вставить** по правой кнопке и `⌘A`;
- из публичного README удалено устаревшее описание старой проблемы камеры.

## 0.19.0

- последнюю вкладку независимого терминала теперь можно закрыть: рабочая область
  показывает понятное пустое состояние и оставляет кнопку подключения с выбором
  сохранённого либо временного сервера;
- SFTP умеет самостоятельно запросить пароль или passphrase в защищённом окне,
  поэтому произвольный сервер больше не требует предварительного входа через
  терминал;
- повторные переходы по папкам SFTP используют устойчивое управляющее
  SSH-соединение; исправлены двойной клик по каталогу и переход на уровень выше;
- файловые панели адаптируются к высоте окна, а очередь передач больше не
  выталкивает нижнюю часть интерфейса за границу экрана;
- добавлен групповой ввод: клавиши выбранной панели можно одновременно
  отправлять во все активные SSH-терминалы;
- добавлена палитра действий `⌘K` для нового подключения, компоновки,
  группового ввода и быстрого открытия выбранного сервера в SFTP;
- из любой терминальной панели можно сразу открыть тот же сервер в
  самостоятельном SFTP-разделе.

## 0.18.3

- кнопка подключения в независимой панели терминала теперь сначала предлагает
  выбрать сохранённый SSH-профиль либо вручную указать hostname/IP, пользователя
  и порт;
- недоступный или удалённый профиль больше не оставляет пустую панель без
  возможности сразу выбрать другой сервер;
- первую вкладку независимого терминала можно закрыть, если открыта хотя бы одна
  другая; оставшаяся вкладка автоматически становится основной;
- закрытие единственной вкладки по-прежнему блокируется, чтобы рабочая область
  не переходила в некорректное пустое состояние.

## 0.18.2

- в каждой отключённой панели терминала теперь видны состояние соединения,
  выбранный сервер и отдельная кнопка подключения;
- добавлен самостоятельный раздел SFTP: локальные файлы доступны сразу, а для
  удалённой панели можно выбрать сохранённый SSH-профиль либо временно указать
  hostname/IP, пользователя и порт;
- сервер автономного SFTP можно сменить без перехода к карточке подключения;
- в настройках оформления появился явный выбор языка интерфейса: системный,
  русский или английский; выбор сохраняется между запусками;
- дополнена английская локализация новых экранов терминала и SFTP.

## 0.18.1

- фокус терминала больше не меняет расположение панелей: выбранная ячейка
  подсвечивается на своём месте и не переносится в левый верхний угол;
- клик непосредственно внутри терминала теперь корректно выбирает его для
  команд панели управления, истории и подключения;
- сетка до четырёх терминалов равномерно заполняет всю доступную рабочую область
  по ширине и высоте;
- панели получили отдельные заголовки, цветные границы и заметную рамку активного
  терминала, поэтому разные серверы легче отличать друг от друга;
- вкладки и заголовки панелей можно перетаскивать мышью, сохраняя выбранный
  пользователем порядок между запусками;
- длинное системное меню компоновки заменено компактной кнопкой с небольшим
  всплывающим окном выбора режима.

## 0.18.0

- исправлено ручное подключение дополнительной SSH-вкладки: после выбора
  `login@host:port` приложение сразу запускает отдельный процесс OpenSSH и
  показывает интерактивный запрос пароля либо passphrase в нужной панели;
- терминал вынесен в самостоятельный раздел бокового меню и больше не зависит
  от выбранной карточки RDP/SSH;
- глобальная рабочая область поддерживает до восьми вкладок, горизонтальное и
  вертикальное разделение, а также сетку до четырёх одновременно видимых панелей;
- каждая вкладка хранит собственный сервер, SSH-профиль, историю, избранное,
  шаблоны и контекстные подсказки;
- добавлен самостоятельный раздел Forwarding: каждое правило локального,
  удалённого или SOCKS-туннеля имеет собственную SSH-цель и не привязано к
  выбранной карточке подключения;
- добавлена английская локализация основных экранов, системных разрешений и
  нового SSH-интерфейса; язык выбирается автоматически по настройкам macOS;
- опубликован отдельный англоязычный README с установкой, возможностями,
  приватностью и правилами работы независимых SSH-туннелей.

## 0.17.8

- каждая дополнительная SSH-вкладка теперь может подключаться к собственному
  серверу, поэтому панели разделённого терминала больше не ограничены одним
  профилем;
- при создании вкладки можно выбрать любой сохранённый SSH-профиль либо указать
  временные hostname/IP, login и port;
- сохранённые профили используют свои ключи, host key policy, compression и
  keepalive, а временные подключения — системные `ssh-agent` и `~/.ssh/config`;
- выбранный сервер и компоновка вкладок восстанавливаются после перезапуска без
  автоматического сетевого подключения;
- история, избранное, шаблоны и контекстные команды изолированы по фактическому
  подключению каждой вкладки;
- сервер работающей вкладки нельзя незаметно заменить: сначала необходимо
  отключить её, что защищает от выполнения команды не на том хосте.

## 0.17.7

- SSH-терминал получил до восьми независимых вкладок, переименование вкладок
  и горизонтальное либо вертикальное разделение рабочей области;
- названия вкладок и компоновка восстанавливаются после запуска, но сетевые
  сессии не переподключаются без действия пользователя;
- команды из истории, общего каталога и контекста сервера можно добавлять в
  избранное отдельно для каждого SSH-профиля;
- добавлены пользовательские шаблоны команд с параметрами вида `${service}`:
  значения запрашиваются при вставке, а команда не запускается автоматически;
- приложение может через ограниченный read-only SSH-запрос обнаружить ОС,
  службы systemd и контейнеры Docker/Podman и предложить подходящие команды;
- сбор контекста ограничен по времени и объёму, не читает содержимое файлов и
  не изменяет состояние удалённого сервера.

## 0.17.6

- встроенный каталог значительно расширен: добавлены команды для файлов,
  поиска, архивов, systemd, журналов, сети, пакетов, дисков, пользователей,
  SSH, Docker, Git, Kubernetes, веб-серверов и баз данных;
- поиск общих команд учитывает не только текст команды, но и категорию,
  назначение и русские ключевые слова;
- перезагрузка внутренней страницы терминала больше не оставляет пустой экран:
  активная SSH-сессия сохраняется, а удерживаемый ANSI-буфер повторно
  передаётся новой странице без отключения и повторного подключения.

## 0.17.5

- исправлено распознавание обычного приглашения SSH-оболочки вида
  `root@server:~#`, поэтому выполненные команды снова сохраняются в истории;
- после выхода из `nano`, `vim` и других полноэкранных программ отслеживание
  строки и всплывающие подсказки автоматически восстанавливаются;
- к вариантам из локальной истории добавлен встроенный каталог общих команд
  для служб, журналов, сети, пакетов, Docker и файлов;
- панель команд получила отдельные вкладки **История** и **Общие команды**;
- парольные запросы с окончанием `:` по-прежнему не считаются командами и не
  записываются в историю.

## 0.17.4

- SSH-терминал сохраняет историю выполненных команд отдельно для каждого
  профиля и восстанавливает её после перезапуска приложения;
- при вводе команды возле курсора появляются подходящие варианты из истории;
  стрелки выбирают вариант, Enter подставляет его в строку без немедленного
  запуска, а Escape закрывает список;
- добавлена отдельная панель истории с поиском, повторной подстановкой,
  удалением отдельных записей, полной очисткой и отключением сохранения;
- история хранится только локально, ограничена по объёму и не записывает строки
  с пробелом в начале, распространёнными признаками секретов или вводом без эха;
- подсказки автоматически скрываются в полноэкранных терминальных программах,
  чтобы не перехватывать их управление.

## 0.17.3

- кнопка оформления больше не становится визуально выбранной сразу после
  запуска приложения;
- публичное README описывает локальное окружение сборки от лица автора, без
  обращения к читателю как к владельцу конкретного журнала или модели Mac;
- описание GitHub Release формируется из этого раздела `CHANGELOG.md`, поэтому
  кнопка **Что нового** всегда открывает содержательный список изменений.

## 0.17.2

- в ARM64-DMG добавлен OpenSSL `legacy.dylib`, содержащий MD4 для
  NTLM/CredSSP-аутентификации FreeRDP;
- RDP-процесс получает `OPENSSL_MODULES`, указывающий на провайдер внутри
  `Selective Remote Session.app`, и больше не зависит от Homebrew на Mac
  пользователя;
- production-сборка проверяет переносимость и подпись провайдера, а финальный
  smoke-тест отклоняет DMG при сообщениях `LEGACY provider failed` или
  `NTLM support not available`;
- публичная версия теперь отображается без внутреннего `CFBundleVersion`;
- окно обновлений получило кнопку **Что нового**;
- каждый выпуск публикуется под новым тегом как отдельный стабильный Release,
  отмеченный **Latest**; повторное использование существующего Release
  блокируется workflow.

## 0.17.1

- проверка обновлений больше не показывает модальное окно до завершения запроса;
- возле кнопки обновления отображается неблокирующий индикатор выполнения;
- повторный запуск проверки блокируется до окончания текущего запроса;
- главная страница проекта получила навигацию, а полный журнал версий вынесен
  в отдельный файл.

### Добавлено в GitHub release v9.3

- обычные сборки используют официальный канал обновлений
  `PastFly/Selective-Remote` без дополнительной переменной окружения;
- добавлена тихая автоматическая проверка обновлений раз в сутки и прямая
  кнопка скачивания DMG;
- GitHub Actions собирает ARM64-DMG и публикует его вместе с SHA-256 в Release;
- при наличии Apple Secrets тот же workflow подписывает приложение сертификатом
  Developer ID, нотариализует DMG и прикрепляет ticket;
- версия приложения: **0.17.1 (build 66)**.

### Исправлено в GitHub release v9.3.1

- CI и Release DMG явно выбирают Xcode 16.2 со Swift 6 на runner `macos-14`;
- сборка больше не использует выбранный по умолчанию Swift 5.10, который не
  умеет читать пакет с `swift-tools-version: 6.0`;
- минимальная целевая версия приложения остаётся macOS 14.

### Исправлено в GitHub release v9.3.2

- устранена ошибка строгой проверки Swift 6 в очереди вывода встроенного
  терминала: захваченный `self` теперь используется явно;
- CI и Release DMG проходят этот этап компиляции одинаково.

### Исправлено в GitHub release v9.3.3

- callback внутреннего SFTP drag-and-drop возвращается на `MainActor` перед
  вызовом completion-handler `NSItemProvider`;
- устранена ошибка actor isolation в production-сборке Swift 6.

### Исправлено в Public preview v9.2

- левый Command→Ctrl и левый Option→Windows снова передаются штатным механизмом
  FreeRDP, поэтому ⌘C/⌘V и Option работают в RDP;
- одновременно используется не больше двух официально поддерживаемых `remap`,
  поэтому исправление не возвращает системный код 22;
- проект подготовлен к открытой публикации: MIT, CI, документация, шаблоны
  issues/PR и проверка на приватные данные;
- версия приложения: **0.17.1 (build 65)**.

## Исправлено в Productivity v9.1

- включение **Option работает как Windows** больше не добавляет новые правила
  в `/kbd`: FreeRDP всегда получает ровно один служебный `remap`, поэтому его
  парсер не завершает RDP с системным кодом 22;
- Command, Option и собственные правила клавиатуры теперь преобразуются в
  безопасном AppKit/SDL-модуле до передачи событий FreeRDP;
- кнопки плавающей панели RDP получили постоянные видимые названия и подробные
  подсказки; переподключение, отключение и переключатели каналов также имеют
  понятные hover-подсказки;
- версия приложения: **0.17.1 (build 64)**.

## Добавлено в Productivity v9

- SFTP получил постоянную очередь передач: отдельный прогресс и скорость для
  каждого объекта, паузу, продолжение, отмену, повтор с `reget`/`reput`, а также
  правила конфликтов **сохранить оба / заменить / пропустить**; очередь не
  останавливается при переходе между SFTP, терминалом и другими вкладками;
- добавлена плавающая панель активных RDP-сессий, доступная поверх полноэкранных
  Spaces: Windows, смена языка, Alt+Tab, Ctrl+Alt+Delete, Print Screen,
  полный экран/окно, переподключение и отключение;
- звук, микрофон и камера меняются из панели и безопасно применяются автоматическим
  переподключением, поскольку FreeRDP создаёт эти каналы при старте сессии;
- правила клавиатуры теперь настраиваются по профилю: Command→Ctrl,
  правый Command→Windows, Option→Windows, Fn→Win+Space и собственные
  переназначения служебных клавиш;
- добавлены фиксированное окно и окно с динамическим разрешением FreeRDP;
- меню macOS показывает избранные профили для быстрого подключения, команды
  активных RDP-сессий и управление SFTP-передачами;
- управление RDP передаётся отдельному процессу через приватный FIFO и штатную
  очередь SDL; внутренние функции FreeRDP по-прежнему не перехватываются;
- версия приложения: **0.17.1 (build 63)**.

## Добавлено в Fn/permissions v8

- одиночное нажатие **Fn** в активном RDP-окне отправляет **Win+Space** и
  переключает раскладку Windows; сочетания Fn с другими клавишами не запускают
  переключение;
- новый Fn-модуль не перехватывает функции FreeRDP: он добавляет обычные
  клавиатурные события в штатную очередь SDL, а сборка отдельно запрещает
  появление таблицы FreeRDP/DYLD interpose в этом модуле;
- отказ macOS в доступе к микрофону или камере больше не завершает RDP-процесс:
  подключение продолжается без соответствующего канала;
- недоступные папки не передаются в Windows и не мешают запуску рабочего стола;
- версия приложения: **0.17.1 (build 62)**.

## Исправлено в RDP recovery v7

- удалён экспериментальный Fn input-модуль, после которого RDP-сеанс мог
  зависнуть сразу после создания окна;
- восстановлен проверенный путь запуска RDP из рабочего патча v4;
- SFTP, SSH-туннели, Retina/fullscreen и переназначение ⌘→Ctrl сохранены;
- версия приложения: **0.17.1 (build 61)**.

## Исправлено в патче Retina/keyboard v4

- смешанная связка Full HD + Retina больше не применяет глобальный
  `FREERDP_WLROOTS_HACK` к настоящим окнам RDP: скрытые monitor-probe получают
  логические размеры отдельно, а полноэкранные окна сохраняют собственную
  плотность пикселей каждого дисплея;
- добавлен профильный переключатель **Использовать ⌘ как Ctrl в Windows**,
  включённый по умолчанию; обе клавиши Command передаются как Ctrl, поэтому
  ⌘C/⌘V/⌘X/⌘A работают привычным для macOS способом;
- старые профили автоматически получают новое поведение, а переключатель можно
  отключить, чтобы снова передавать Command как клавишу Windows.

**Selective Remote** — самостоятельное нативное приложение macOS для RDP-, SSH-
и SFTP-профилей. RDP поддерживает визуальный выбор произвольного набора
физических дисплеев, а SSH — встроенный терминал, двухпанельный SFTP, системные
ключи и port forwarding. Пакет приложения называется `Selective Remote.app`,
исполняемый файл и служебные артефакты используют единое имя `SelectiveRemote`.

## Исправлено в 0.17.1

- завершено внутреннее переименование: `.app`, bundle identifiers, исполняемые
  файлы, helpers, журналы и настройки больше не используют прежнее имя;
- SFTP сохраняет подключение, текущие каталоги и фильтры при переходе между
  вкладками **SFTP** и **Терминал**;
- добавлены фильтры по имени, видимый столбец даты изменения, создание пустых
  файлов и папок через контекстное меню пустой области;
- одиночный щелчок явно выделяет файлы и папки, а редактор POSIX-прав получил
  понятные галочки чтения, записи и выполнения для владельца, группы и остальных;
- перенос удалённых файлов и папок использует ленивое файловое представление
  штатного `NSItemProvider`, совместимое со SwiftUI drag-and-drop;
- устранена ложная надпись «Удалённая папка пуста», когда OpenSSH возвращает
  непустой каталог в варианте `ls -l`, отличающемся от прежнего строгого шаблона;
- SFTP-парсер теперь принимает числовой или неизвестный link count, UID/GID
  вместо имён, ACL-маркеры, ведущие пробелы, локализованные месяцы, традиционную
  и ISO-дату, сохраняя пробелы в именах файлов; полные пути из ответа OpenSSH
  корректно приводятся к именам объектов текущего каталога;
- если будущая версия сервера вернёт действительно неизвестный формат,
  приложение покажет диагностическую ошибку с безопасным фрагментом ответа,
  а не выдаст непустую папку за пустую;
- добавлены регрессионные тесты на каталог Debian `/etc`, приглашение OpenSSH,
  ACL-маркеры, неизвестный link count и ISO-дату;
- версия приложения: **0.17.1 (build 58)**.

## Добавлено и изменено в 0.17.0

- SFTP превращён в полноценный двухпанельный проводник: появились история
  **назад/вперёд**, кликабельные хлебные крошки и адресная строка для прямого
  перехода к локальному или серверному пути;
- в списках сразу видны владелец с группой, символьные POSIX-права и размер;
  сортировка работает по имени, размеру, дате изменения, владельцу и доступу,
  папки при этом остаются сверху;
- правый клик открывает контекстное меню: открыть, открыть с помощью, встроенно
  редактировать удалённый UTF-8-файл, скопировать между панелями, переименовать,
  показать в Finder, удалить и открыть свойства;
- окно свойств показывает полный путь, тип, размер, дату, владельца, группу,
  символьный и восьмеричный режим; из него можно выполнить `chmod`, а при наличии
  серверных прав — сменить UID/GID через `chown` и `chgrp`;
- локальное удаление отправляет объект в Корзину; серверное удаление требует
  отдельного подтверждения и для папок допускается только при пустом каталоге;
- встроенный редактор ограничен текстовыми UTF-8-файлами до 5 МБ и при сохранении
  не переносит права временного файла на сервер;
- начат совместимый ребрендинг: видимое имя — **Selective Remote**, подпись —
  **RDP · SSH · SFTP**, акцент смещён к сине-бирюзовой палитре и сетевому знаку;
- версия приложения: **0.17.0 (build 57)**.

## Исправлено в 0.16.1

- устранена ошибка компиляции Xcode 26.5: внутреннее состояние прозрачности больше не конфликтует с унаследованным `NSView.appearance`;
- добавлен регрессионный тест, запрещающий повторное объявление конфликтующего свойства;
- темы, прозрачность окна и исправление `ssh-agent` из 0.16.0 сохранены без изменения функциональности;
- версия приложения: **0.16.1 (build 56)**.

## Добавлено и исправлено в 0.16.0

- набор терминальных тем расширен до 14 вариантов: добавлены Tokyo Night, Nord, One Dark, Gruvbox Dark, Catppuccin Mocha, Monokai, Rosé Pine и Solarized Light;
- оформление теперь открывается не только из терминала, но и глобальной кнопкой с палитрой в боковой панели;
- добавлен режим прозрачного окна с системным размытием и сохраняемой настройкой непрозрачности от 55 до 100%;
- SFTP и forwarding больше не требуют повторно загружать выбранный ключ в `ssh-agent`, если для профиля уже работает встроенная SSH-сессия: используется её защищённый OpenSSH ControlPath;
- при запуске SelectiveRemote из Finder без доступного `SSH_AUTH_SOCK` приложение ищет агент пользовательской launchd-сессии, а затем при необходимости запускает собственный `/usr/bin/ssh-agent`;
- сокет резервного агента создаётся в уникальном каталоге с правами `0700`, передаётся только дочерним SSH/SFTP-процессам и удаляется при завершении приложения;
- устранено предупреждение Swift 6.3 о доступе к `NSColorWell.color` вне MainActor;
- версия приложения: **0.16.0 (build 55)**.

## Исправлено в 0.15.1

- устранено падение Apple Swift 6.3.3 `SmallVector unable to grow` на этапе IRGen файла `TerminalAppearance.swift`;
- пользовательские цвета терминала теперь выбираются нативным `NSColorWell` и сохраняются непосредственно в формате `#RRGGBB`, без проблемного bridge `SwiftUI.Color → NSColor`;
- функциональность тем, пользовательских цветов, полноразмерного терминала и исправленной геометрии PTY из 0.15.0 сохранена;
- версия приложения: **0.15.1 (build 54)**.

## Добавлено и исправлено в 0.15.0

- терминал больше не находится внутри ограниченного `ScrollView`: он растягивается на всю доступную ширину и высоту окна;
- кнопка **Развернуть терминал** включает режим «Фокус»: скрываются боковая панель, заголовок профиля, вкладки и нижняя строка; повторное нажатие или `⌘⇧Enter` возвращает интерфейс;
- размер xterm пересчитывается после фактической раскладки SwiftUI/WKWebView и изменения шрифта, а PTY получает стабильные столбцы и строки;
- промежуточная геометрия `2×1`, возникающая во время переключения вкладок, больше не передаётся `nano`, `vim`, `tmux` и другим TUI-программам;
- ANSI/VT-вывод передаётся в WebView строго по порядку небольшими блоками и последовательно разбирается xterm.js; это устраняет повреждение полноэкранной разметки и цветов;
- локальная CSP терминала разрешает динамические стили, которые xterm.js 6 использует для canvas/DOM-геометрии; скрипты и внешняя сеть остаются запрещены;
- в панели **Оформление** доступны темы Midnight, Hacker Green, Solarized Dark, Dracula, Light и пользовательская тема;
- настраиваются фон, цвет текста, курсор, шрифт, размер, межстрочный интервал, форма и мигание курсора, внутренний отступ;
- настройки терминала сохраняются для следующих запусков и применяются также к отдельному терминалу генерации SSH-ключа;
- строка состояния терминала показывает фактический размер PTY в столбцах и строках;
- системное хранилище ключей ищется через LaunchServices по bundle identifier, затем по путям Keychain Access для разных macOS; при отсутствии отдельной «Связки ключей» используется приложение «Пароли»;
- отсутствие внешнего GUI хранилища больше не выдаётся за неисправность Keychain: SSH-ключами SelectiveRemote можно управлять во встроенном окне;
- версия приложения: **0.15.0 (build 53)**.

## Исправлено в 0.14.1

- устранена ошибка компиляции macOS: `NSFilePromiseProvider` больше не выдаётся за `NSItemProvider`;
- выгрузка удалённых файлов и папок в Finder теперь использует ленивую файловую репрезентацию штатного `NSItemProvider`;
- служебная копия загружается по SFTP только после фактического drop и удаляется из временного каталога позднее;
- внутренний drag-and-drop между удалённой и локальной панелями сохраняет отдельный безопасный payload;
- версия приложения: **0.14.1 (build 52)**.

## Добавлено и исправлено в 0.14.0

- SFTP получил двухпанельный файловый менеджер: слева локальный Mac, справа удалённый сервер;
- файлы и папки можно переносить между панелями кнопками или drag-and-drop, включая загрузку из Finder и экспорт удалённых объектов обратно в Finder;
- для удалённых папок используются рекурсивные операции SFTP, а новые локальные копии получают безопасное свободное имя вместо незаметной перезаписи;
- локальная панель умеет переходить домой, вверх, в выбранную через системный диалог папку, обновляться и создавать каталоги;
- SFTP и port forwarding могут повторно использовать уже аутентифицированную парольную SSH-сессию через отдельный защищённый OpenSSH control socket; пароль по-прежнему не сохраняется;
- сокет привязан к профилю, host, пользователю и порту, поэтому после изменения подключения старая сессия не будет использована для другого сервера;
- генерация SSH-ключа выполняется в отдельном встроенном PTY и больше не требует завершать текущую SSH-сессию;
- добавлены регрессионные тесты multiplexing и рекурсивных команд SFTP;
- версия приложения: **0.14.0 (build 51)**.

## Исправлено в 0.13.2

- удалён зависающий во время сборки диалоговый запуск системного `ssh-keygen`;
- PTY проверяется собственным детерминированным helper-процессом: тест синхронно проверяет вывод, ввод, Enter и изменение размера без внешних диалогов;
- аргументы реальной генерации SSH-ключа теперь отдельно проверяются Swift-тестом: passphrase по-прежнему вводится только во встроенном терминале и отсутствует в аргументах процесса;
- в журнал сборки добавлены явные сообщения до и после PTY-проверки;
- версия приложения: **0.13.2 (build 50)**.

## Исправлено в 0.13.1

- устранено ложное падение нативного PTY-теста после успешной Release-сборки: тест теперь читает ожидаемый вывод до ожидания завершения дочернего процесса и не попадает в гонку с неблокирующим PTY macOS;
- каталог встроенного xterm.js явно исключён из ресурсов SwiftPM, поскольку `build_app.sh` самостоятельно копирует его в bundle; предупреждения о восьми необработанных файлах больше нет;
- функциональность приложения не изменена;
- версия приложения: **0.13.1 (build 49)**.

## Добавлено в 0.13.0

- SSH больше не передаётся macOS как ссылка `ssh://`: внешние приложения и обработчики не запускаются;
- в SelectiveRemote встроен полноценный терминальный экран на xterm.js 6.0.0 с цветом, Unicode, прокруткой, копированием, вставкой и изменением размера;
- системный `/usr/bin/ssh` запускается напрямую, без shell-строк, внутри собственного PTY; работают интерактивные password/passphrase/host-key запросы и TUI-программы;
- добавлена безопасная генерация ключей **Ed25519** и **RSA 4096** через системный `ssh-keygen`; путь выбирается заранее, существующие файлы никогда не перезаписываются;
- passphrase нового ключа вводится только в PTY: он отсутствует в аргументах процесса, окружении, профиле и журнале;
- выбранный публичный ключ можно интерактивно добавить на сервер через системный `ssh-copy-id`; пароль сервера вводится во встроенном терминале;
- окно **Keychain и SSH-ключи** получило команды создания и установки ключа;
- активные встроенные SSH-сессии отображаются в списке профилей, завершаются из строки состояния, контекстного меню и меню приложения;
- SFTP, port forwarding и весь RDP-контур 0.12.0 сохранены;
- версия приложения: **0.13.0 (build 48)**.

## Добавлено в 0.12.0

- отдельные типы профилей **RDP** и **SSH** с автоматической миграцией всех старых профилей в RDP;
- безопасный запуск интерактивного SSH в системном Terminal через `ssh://`, без shell-строк и без сохранения пароля сервера;
- настройки SSH-порта, пользователя, сжатия, keepalive, начальной папки SFTP и политики проверки host key;
- встроенный SFTP-браузер: навигация по папкам, обновление, загрузка, скачивание и создание каталогов;
- local (`-L`), remote (`-R`) и dynamic SOCKS (`-D`) forwarding; одновременно можно держать несколько туннелей, каждый со своим журналом;
- окно **Keychain и SSH-ключи**: регистрация существующего приватного ключа, fingerprint, переименование, показ в Finder, копирование `.pub` и управление `ssh-agent`;
- passphrase передаётся штатному `ssh-add --apple-use-keychain` через отдельный защищённый askpass-helper и затем загружается через `--apple-load-keychain`;
- профили и туннели используют системные `~/.ssh/config`, `known_hosts` и `ssh-agent`; собственный SSH-стек и сторонние сетевые библиотеки не добавлялись;
- экспорт профилей включает параметры SSH/SFTP/forwarding, но не включает пароли, passphrase, приватные ключи или привязку к локальному файлу ключа;
- сохранены без изменений весь RDP-контур 0.11.1, выбор дисплеев, Retina/fullscreen, камера, микрофон, качество и переподключение;
- версия приложения: **0.12.0 (build 47)**.

## Исправлено в 0.11.1

- устранено ложное окно ошибки «FreeRDP за 8 секунд не начал инициализацию», когда RDP-сессия уже открылась;
- готовность определяется не только по `Local framebuffer format` в журнале, но и по реальному окну процесса **Selective Remote Session** в WindowServer;
- обнаружение работает для полноэкранного окна на другом macOS Space и не требует разблокировки буферизованного INFO-вывода FreeRDP;
- небольшие системные и служебные диалоги не выдаются за готовое RDP-окно;
- настоящий долгий запуск теперь отображается ненавязчивым статусом, а не модальной ошибкой, пока процесс FreeRDP остаётся активным;
- источник определения готовности записывается в session-лог для последующей диагностики;
- добавлен регрессионный тест по фактическому размеру окна из проблемной сессии;
- версия приложения: **0.11.1 (build 46)**.

## Добавлено в 0.11.0

- в разделе **Дисплеи → Качество RDP-сессии** появился отдельный сетевой профиль для каждого подключения;
- доступны режимы **Автоматически**, **Макс. качество**, **Сбалансировано** и **Экономия**;
- явные режимы используют штатные параметры FreeRDP `/network:lan`, `/network:broadband-high` и `/network:broadband-low`, которые подбирают визуальные эффекты под пропускную способность;
- режим **Автоматически** не добавляет новый аргумент и полностью сохраняет поведение профилей из предыдущих версий;
- выбранный режим сохраняется при экспорте архива SelectiveRemote, а старые профили мигрируют автоматически;
- фактический режим и переданный параметр сети записываются в начало журнала каждой сессии;
- добавлены регрессионные тесты миграции и точного преобразования всех четырёх режимов в аргументы SDL-FreeRDP;
- версия приложения: **0.11.0 (build 45)**.

## Добавлено в 0.10.0

- в разделе **Устройства и звук** появилась проверка камеры и микрофона до запуска RDP;
- предпросмотр открывает именно источник, выбранный в текущем профиле, и применяет соответствующий режим качества;
- если сохранённая камера недоступна, окно проверки заранее показывает фактическую резервную камеру и предупреждает о переходе;
- отдельно отображаются статусы разрешений macOS для камеры и микрофона, активный системный микрофон и количество найденных камер;
- доступ можно запросить заранее, а при запрете — одним нажатием открыть нужный раздел системных настроек;
- предпросмотр запускается только по явному нажатию, ничего не записывает и освобождает камеру при остановке или закрытии окна;
- проверка блокируется, пока запущена хотя бы одна RDP-сессия, чтобы локальный предпросмотр не конкурировал с RDPECAM за устройство;
- AVFoundation-сеанс изолирован на последовательной очереди, поэтому запуск, перенастройка и остановка камеры не пересекаются;
- добавлены тесты преобразования TCC-статусов и соответствия профилей качества форматам предпросмотра;
- версия приложения: **0.10.0 (build 44)**.

## Исправлено в 0.9.1

- устранён аварийный `Abort trap: 6` с маркером `__TCC_CRASHING_DUE_TO_PRIVACY_VIOLATION__` при первом запросе доступа к микрофону;
- описание использования микрофона и entitlement Audio Input теперь есть как у внешнего **Selective Remote**, так и у вложенного **Selective Remote Session**: macOS может назначить ответственным за запрос любой из этих bundle в зависимости от способа запуска и подписи;
- скрипт сборки проверяет непустые описания микрофона и камеры в обоих `Info.plist`, а после подписи проверяет Camera и Audio Input entitlements в обоих приложениях; неполный DMG больше не создаётся;
- TCC-abort классифицируется по последнему активному запросу и показывает правильную подсказку отдельно для микрофона и камеры;
- версия приложения: **0.9.1 (build 43)**.

## Добавлено в 0.9.0

- в каждом профиле можно выбрать встроенную камеру Mac, системную камеру macOS или конкретное устройство — включая USB- и Continuity-камеры iPhone;
- RDPECAM передаёт Windows только выбранную камеру, поэтому удалённая программа больше не выбирает случайно первое устройство по хешу;
- если сохранённая камера недоступна, используется встроенная камера Mac, затем системная; причина перехода явно записывается в журнал;
- добавлены четыре режима качества камеры: **Экономия** (640×480, 15 FPS, цель 0,7 Мбит/с), **Сбалансированное** (1280×720, 30 FPS, цель 1,25 Мбит/с), **Высокое** (1920×1080, 30 FPS, цель 2,7 Мбит/с) и **Автоматически**;
- фиксированные режимы передают удалённой стороне один лучший поддерживаемый камерой формат в заданных пределах и задают целевой битрейт H.264; фактический трафик может отклоняться в зависимости от кодировщика, изображения и сервера;
- список камер автоматически обновляется при подключении и отключении устройств; доступно также ручное обновление;
- интерфейс показывает фактическое имя и тип выбранной камеры, а журнал — безопасный идентификатор, имя, тип, режим выбора, качество и согласованный формат;
- NV12-буфер кадра теперь выделяется один раз на поток и переиспользуется вместо создания нового массива до 3 МБ для каждого кадра 1080p;
- устаревший общий вызов перечисления AVFoundation заменён на `AVCaptureDeviceDiscoverySession`, Continuity Camera объявлена штатным типом устройства;
- старые профили мигрируют без действий пользователя: камера по умолчанию — встроенная, качество — сбалансированное;
- версия приложения: **0.9.0 (build 42)**.

## Исправлено в 0.8.10

- перед запуском FreeRDP вложенное приложение **Selective Remote Session** теперь само проверяет доступ к микрофону и камере и, если нужно, ожидает ответа в системном диалоге;
- первый запуск с включённым микрофоном больше не отменяется строкой `audin_mac_close: not authorized`, пока пользователь отвечает на запрос macOS;
- отказ, системное ограничение и тайм-аут разрешения распознаются отдельно для микрофона и камеры и показывают точный путь к нужному разделу настроек;
- открытый системный privacy-диалог больше не выдаётся за зависание FreeRDP; восьмисекундный контроль запуска начинается заново после выдачи разрешений;
- предварительная проверка загружается только для профилей с включённым микрофоном или доступной камерой и не затрагивает обычные RDP-сессии и определение мониторов;
- ненужные privacy-описания удалены из основного приложения: разрешения принадлежат только фактическому потребителю устройств — **Selective Remote Session**;
- добавлены тесты конфигурации preflight и классификации реальной ошибки `audin`;
- версия приложения: **0.8.10 (build 41)**.

## Изменено в 0.8.9

- состояние **«Подключено»** теперь устанавливается только после создания удалённого framebuffer, а не по первой служебной строке журнала;
- предупреждение о долгом запуске автоматически снимается, если удалённый рабочий стол всё же успешно открылся;
- одиночный `ERRCONNECT_CONNECT_CANCELLED` больше не скрывается как штатное завершение без подтверждения `Connection aborted by user`;
- список SDL-мониторов кэшируется до изменения конфигурации дисплеев, поэтому запуск следующих параллельных сессий не выполняет лишний `/list:monitor`;
- наличие camera addin определяется один раз вместо повторного обхода каталога Frameworks при каждом обновлении SwiftUI;
- запись профилей в UserDefaults объединяется с задержкой 300 мс, поэтому ввод текста больше не сериализует весь список на каждое нажатие клавиши; при завершении приложения данные сохраняются немедленно;
- поиск профилей больше не создаёт четыре временные lowercase-строки на каждый профиль;
- сортировка вертикально расположенных мониторов с одинаковой координатой X стала детерминированной;
- удалены неиспользуемые `DisplayDescriptor.scaleFactor`, `DisplayPlacement.sourceFrame` и импорт `ColorSync`;
- чтение вывода `/list:monitor` больше не может заблокироваться из-за заполненного pipe;
- проверка обновлений учитывает `minimumMacOS` и не предлагает несовместимый пакет;
- версия приложения: **0.8.9 (build 40)**.

## Исправлено в 0.8.8

- исправлена первопричина обрезанной панели Windows на единственном Retina-дисплее: глобальный `FREERDP_WLROOTS_HACK=force` больше не подменяет реальный размер fullscreen drawable логическими границами экрана;
- вместо отключения Retina backing используется штатный `/smart-sizing:<ширина>x<высота>` SDL-FreeRDP: удалённый рабочий стол целиком масштабируется в фактическую область полного экрана, включая панель **Пуск**;
- верхнее меню macOS снова доступно, а interposer больше не вмешивается в создание и переключение полноэкранного окна при одном дисплее;
- topology interposer и monitor-probe fallback сохранены только для выбранных многомониторных схем;
- версия приложения: **0.8.8 (build 39)**.
