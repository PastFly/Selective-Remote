## 0.27.0

- SSH-профили и группы получили упорядоченные Startup Snippets: можно добавить до восьми Snippets, менять порядок и запускать последовательность после появления shell prompt.
- Одиночный Startup Snippet остаётся обратно совместимым; последовательности используют существующий SSH runtime и не создают второй движок выполнения.
- Исправлена читаемость checkbox в светлой теме, включая настройки подключений и POSIX-права SFTP.
- History/Snippets используют единый полноразмерный Inspector в SSH single/split/grid, терминале SSH-профиля из «Подключений» и локальном «Терминале».
- Добавлены regression-тесты для Startup Sequence, Inspector UX и checkbox-контраста.

## 0.26.0

> **Важно после обновления — «Объединить пароли».** Если у вас уже есть сохранённые SSH/RDP-пароли, откройте **Связка ключей** и нажмите **«Объединить пароли»**. Selective Remote один раз переносит старые отдельные записи macOS Keychain в единый защищённый Vault. Во время первой миграции macOS ещё может попросить доступ к некоторым старым записям — это требуется только для их чтения. После переноса новые сборки используют одну Vault-запись, поэтому обновление приложения больше не должно превращаться в десятки запросов Keychain для десятков профилей. Расшифрованное содержимое Vault кэшируется только в памяти процесса и исчезает после закрытия приложения.

- Исправлено редактирование Group inheritance: общие значения SSH-группы теперь всегда можно менять, а переключатели наследования отвечают только за применение конкретного значения к текущему профилю.
- Исправлена миграция «Объединить пароли»: устранена ошибка Keychain `-50` за счёт корректного двухэтапного чтения старых записей; частично недоступные записи больше не срывают перенос остальных.
- В контекстное меню хоста добавлено «Изменить»: команда сразу открывает «Подключения → Основное» выбранного профиля из любого рабочего пространства.
- Startup Snippets, Terminal Variables и Group inheritance вынесены в отдельную видимую вкладку «Автоматизация» SSH-профиля; если группа не назначена, интерфейс прямо объясняет, где её включить.
- Добавлен единый Keychain Vault для паролей RDP/SSH/Proxy/Forwarding: после первого доступа секреты читаются из одной Keychain-записи и кэшируются только в памяти до завершения приложения, что устраняет лавину системных запросов после ad-hoc обновлений.
- В разделе «Связка ключей» добавлена однократная команда «Объединить пароли» для пакетного переноса существующих per-profile записей в единый Vault.
- Добавлен Host Insights поверх Server Context: hostname, uptime, load average, RAM, корневой диск, доступные обновления, listening ports и активные пользователи; метрики используют реальные данные удалённого Linux-хоста и дают быстрый переход к диагностическим командам.
- Автодополнение Terminal стало контекстным: учитывает текущий аргумент команды, `sudo`, историю, избранное, Snippets, Server Commands, systemd-службы и Docker/Podman-контейнеры конкретного сервера.
- SSH-профили получили Startup Snippets с режимами «выключено», «спросить» и «автоматически», а также отдельной настройкой запуска после Smart Reconnect.
- Добавлены безопасные Terminal Variables: встроенные `${HOST}`, `${USER}`, `${PORT}`, `${PROFILE}`, `${GROUP}`, `${OS}`, `${OS_ID}` и пользовательские переменные; имена, похожие на PASSWORD/TOKEN/SECRET/API_KEY, намеренно запрещены — секреты остаются в Keychain.
- Группы SSH теперь могут наследовать отдельные параметры: username, порт, Jump Host, keepalive, Startup Snippet и переменные; каждый параметр можно независимо переопределить в конкретном профиле.
- Добавлены Named Workspaces для SSH и локального Terminal: сохраняются вкладки, их порядок, split/grid, подключения или рабочие папки и полное индивидуальное оформление каждой панели.
- Загрузка Named Workspace безопасна: она разрешена только при отсутствии активных терминальных сессий, восстанавливает структуру в отключённом состоянии и никогда не инициирует SSH-подключения автоматически.
- Добавлены regression-тесты для Host Insights, контекстного autocomplete, Startup Snippets, переменных, группового наследования, persistence и безопасного восстановления Named Workspaces.

## 0.25.0

- Панель Snippets в локальном и SSH-терминале больше не закрывается после выполнения или вставки сниппета; панель остаётся открытой до явного закрытия пользователем.
- Ручная проверка обновлений теперь полностью остаётся внутри окна «Настройки»: оно не закрывается и не теряет фокус из-за системного alert; состояние и результат проверки отображаются inline.
- Размер и визуальный масштаб кнопок правой панели локального терминала приведены к SSH-интерфейсу.
- Каждая локальная и SSH-вкладка получила полные индивидуальные настройки терминала: весь каталог тем с тем же предпросмотром, шрифт, размер, межстрочный интервал, курсор, подсветка синтаксиса, отступы и своя палитра.
- Индивидуальные настройки сохраняются по стабильному UUID вкладки, восстанавливаются вместе с Terminal Workspace и могут в один клик скопировать текущее общее оформление.
- Добавлены регрессионные тесты для Snippets, блокировки окна обновлений, размеров локального toolbar и per-pane тем.
- Исправлен fullscreen RDP на одном мониторе macOS: системные menu/title-bar controls снова появляются при наведении вверх, а hidden SDL probe отдельно учитывает только верхнюю safe-area/menu bar при расчёте высоты удалённого рабочего стола, чтобы панель задач Windows не уходила ниже видимой области.

## 0.24.0

- Добавлены пользовательские теги подключений: один профиль может иметь несколько тегов, доступны создание, назначение, глобальное переименование, удаление, поиск и составная фильтрация.
- Каталог подключений получил переключение между списком и компактной плиткой; расширяемая боковая панель вмещает больше карточек в строке.
- Добавлен App Lock с Touch ID: блокировка при запуске, после сна, сворачивания или бездействия, а также вручную через `⌘⇧L`.
- Исправлен crash LocalAuthentication после успешного Touch ID на Swift 6; проверка переведена на безопасный `async/await` с возвратом на MainActor.
- Закрыты обходы App Lock через отдельное окно настроек, меню «Сессия», menu-bar extra и сочетание `⌘T`; отключение защиты в заблокированном состоянии требует системной аутентификации.
- Добавлен локальный журнал RDP- и SSH-подключений с фильтрами, поиском и очисткой; журнал не сохраняет пароли, ключи, команды или содержимое терминала и очищает потенциальные секреты в сообщениях об ошибках.
- В меню «Справка» сохранён пункт «Поддержать проект» с существующими вариантами поддержки без введения подписок или платных планов.
- Добавлен отдельный CI-workflow для безопасной сборки тестового ARM64 DMG без создания тега или production-релиза.

## 0.23.0

- Библиотека Snippets получила корневой экран групп, навигацию назад, сохранение выбранной папки и переключение между списком и плиткой.
- Исправлено схлопывание списка при клике по пустой области: браузер больше не зависит от нестабильного `List(selection:)` и не выбирает первый сниппет принудительно.
- Связь сниппета с группой переведена с имени на стабильный `groupID`; существующая библиотека мигрирует автоматически без потери команд и Targets.
- В панели Snippets внутри SSH и локального терминала добавлены «Выполнить здесь», «Вставить без запуска» и «Скопировать команду»; запуск на назначенных Targets сохранён.
- Текущий глобальный удалённый Terminal переименован в SSH, а новый раздел «Терминал» запускает локальный login shell текущего пользователя.
- Локальный терминал поддерживает до восьми вкладок, рабочую папку для каждой вкладки, историю, темы, Snippets и системное сочетание `⌘T`.
- PTY runtime получил безопасную поддержку рабочей директории без изменения процесса приложения.
- Добавлено системное окно «Настройки» с разделами «Оформление» и «Обновления», текущей/доступной версией, историей изменений, ручной проверкой и переключателями автоматической проверки и загрузки.
- Тема, размер текста и плотность интерфейса теперь применяются также к Справке, Настройкам и окну «Что нового»; прозрачность изменяет фон, не приглушая текст и элементы управления.
- Добавлены миграционные, persistence-, PTY-, SwiftUI- и browser-регрессии для новой модели.

## 0.22.6

- Добавлена глобальная библиотека Snippets, общая для всех Terminal Workspace и SSH-профилей.
- Один сниппет можно назначить на восемь сохранённых SSH Targets; существующие шаблоны автоматически мигрируют без потери данных.
- Запуск сниппета повторно использует активную SSH-сессию либо подключает нужный Target и отправляет команду после готовности shell.
- Добавлены группы, поиск, создание, редактирование, дублирование, перемещение и исправленное удаление сниппетов.
- Добавлены запуск двойным кликом, через Enter и контекстное меню, а также создание сниппета внутри пустой группы.
- Глобальное окно показывает состояние отправки по каждому Target и позволяет вручную отключить связанные SSH-сессии с подтверждением.
- Редактор Snippets внутри терминала теперь поддерживает назначение нескольких Targets.
- Добавлены миграционные, persistence-, bridge- и browser-регрессии для глобальных Snippets.

## 0.22.5

- Исправлено аварийное завершение приложения при drag-and-drop Server → Server и Server → Mac в release-сборках.
- Внутренний SFTP drag-and-drop больше не читает строковый payload через проблемный NSItemProvider Data callback.
- Строковый drag payload теперь загружается через NSString/object representation, совместимую с production-сборкой на Xcode 16.2.
- Исправление проверено на DMG, собранном GitHub Actions тем же toolchain, что используется для production Release.

## 0.22.4

- Исправлено аварийное завершение приложения при drag-and-drop файлов между двумя SFTP-серверами.
- NSItemProvider callbacks полностью вынесены из MainActor-изолированных SwiftUI представлений в отдельный non-actor bridge.
- Обработка данных drag-and-drop теперь возвращается на MainActor только после получения Sendable-значения от Foundation.
- Исправление применяется также к Mac ↔ Server и Server → Mac путям передачи файлов.
- Добавлены runtime regression tests для асинхронной доставки NSItemProvider и actor isolation.

## 0.22.3

- Исправлено аварийное завершение приложения при drag-and-drop файлов между двумя SFTP-серверами.
- Исправлена actor isolation для NSItemProvider callbacks: обработка drag-and-drop теперь безопасно переключается на MainActor перед обращением к SFTP Workspace.
- Добавлен regression test для защиты Server → Server drag-and-drop от повторного появления ошибки.

## 0.22.2

- Стабилизирован жизненный цикл SFTP Workspace: активные передачи корректно отменяются при отключении или закрытии панели.
- Исправлено управление общими OpenSSH ControlMaster-сессиями между несколькими SFTP-панелями.
- При удалении SSH-профиля теперь также удаляются связанные сохранённые proxy credentials.
- Connection Center переведён на реальные SFTP Workspace panes и поддерживает открытие, отключение и переподключение конкретной панели.
- Исправлен drag-and-drop Mac ↔ Server и Server ↔ Server.
- Исправлена компоновка SFTP после обновления Transfer Queue; расширенная история передач ограничена отдельной прокручиваемой областью.
- Удалён устаревший single-session SFTP runtime, legacy observers и старый SFTPBrowserView; AppModel.sftpWorkspace теперь является единым источником состояния SFTP.
- Release workflow больше не содержит устаревший default tag и требует явного указания версии релиза.
- RDP monitor topology и hot-plug логика в этом релизе не изменялись.

## 0.22.1

- Исправлено восстановление полноэкранного multi-monitor RDP при подключении и отключении физических дисплеев: Selective Remote выполняет контролируемое переподключение с актуальным набором выбранных мониторов вместо зависших, устаревших или прыгающих окон.
- Сохраняются основной монитор и виртуальный порядок дисплеев при hot-plug: схема `Mac – HP1 – HP2` после отключения среднего монитора остаётся `Mac – HP2`, даже если macOS переназначает локальные координаты экранов.
- Для конфликтующей локальной раскладки добавлено временное application-scoped выравнивание через CoreGraphics; постоянная конфигурация дисплеев macOS не изменяется.
- Исправлен clamshell-сценарий: при закрытии крышки MacBook с внешними мониторами RDP перестраивается на доступные дисплеи без ошибки `SDL:Invalid display`.
- Сокращено ожидание перед восстановлением RDP после отключения внешнего монитора; при возвращении выбранного дисплея автоматически восстанавливается полная сохранённая схема.
- Исправлен режим «Окно · менять разрешение»: SDL monitor probes больше не срывают запуск, а dynamic resolution используется для изменения разрешения вслед за размером окна.
- Улучшена обработка ожидаемых RDP-разрывов при перестройке мониторов и сообщения об ошибках подключения; пользовательское отключение не запускает Smart Reconnect.
- Добавлены regression tests для hot-plug, clamshell, порядка мониторов, dynamic window и Smart Reconnect.

## 0.22.0

- Добавлено аппаратное декодирование H.264 для RDP при доступности VideoToolbox.
- Улучшена обработка clipboard и keyboard mapping для macOS ↔ Windows.
- Добавлена поддержка multi-monitor RDP с выбором физических дисплеев и виртуальной раскладкой.
- Добавлена настройка масштаба Windows и качества RDP.

## 0.21.16

- Исправлена панель быстрого запуска SFTP и её интеграция с SSH-профилями.

## 0.21.15

- Улучшена обработка reconnect и диагностика ошибок SSH.

## 0.21.14

- Исправлены edge cases отображения терминала и навигации.

## 0.21.13

- Улучшена стабильность RDP и SFTP.

## 0.21.12

- Исправлены мелкие ошибки UI и релизного workflow.

## 0.21.11

- Улучшена надёжность хранения профилей и Keychain-интеграции.

## 0.21.10

- Исправлена обработка аргументов FreeRDP и OpenSSH.

## 0.21.9

- Улучшена диагностика подключений.

## 0.21.8

- Исправлена логика SFTP transfer queue.

## 0.21.7

- Улучшены терминальные темы и цветовая схема.

## 0.21.6

- Исправлены мелкие ошибки интерфейса.

## 0.21.5

- Улучшена работа с несколькими мониторами.

## 0.21.4

- Исправлены проблемы жизненного цикла RDP-сессий.

## 0.21.3

- Улучшено управление SSH-ключами.

## 0.21.2

- Исправлены ошибки импорта профилей.

## 0.21.1

- Добавлены улучшения безопасности и диагностики.

## 0.20.18

- Улучшена стабильность приложения.

## 0.20.17

- Исправлены проблемы RDP на Retina-дисплеях.

## 0.20.16

- Улучшена обработка полноэкранного режима.

## 0.20.15

- Исправлены ошибки SSH и SFTP.

## 0.20.14

- Улучшена интеграция Keychain.

## 0.20.13

- Исправлены мелкие UI-проблемы.

## 0.20.12

- Улучшена стабильность терминала.

## 0.20.11

- Исправлены ошибки RDP.

## 0.20.10

- Улучшена работа с несколькими дисплеями.

## 0.20.9

- Исправлены мелкие ошибки.

## 0.20.8

- Улучшена производительность.

## 0.20.7

- Исправлена стабильность соединений.

## 0.20.6

- Улучшена диагностика.

## 0.20.5

- Исправлены терминальные темы.

## 0.20.4

- Улучшена работа SSH.

## 0.20.3

- Исправлена работа SFTP.

## 0.20.2

- Улучшено управление профилями.

## 0.20.1

- Исправлены проблемы UI.

## 0.20.0

- Крупное обновление Selective Remote.
