# Selective Remote

[![CI](https://github.com/PastFly/Selective-Remote/actions/workflows/ci.yml/badge.svg)](https://github.com/PastFly/Selective-Remote/actions/workflows/ci.yml)
[![macOS 14+](https://img.shields.io/badge/macOS-14%2B-blue)](https://support.apple.com/macos)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

Нативный клиент удалённого доступа для macOS: RDP с несколькими мониторами,
SSH-терминал, двухпанельный SFTP и SSH-туннели в одном приложении.

## Возможности

- RDP через SDL-FreeRDP, полноэкранный режим на нескольких дисплеях и Retina;
- привычные сочетания macOS: левый Command как Ctrl, правый Command как Windows,
  левый Option как Windows и Fn для переключения языка;
- перенаправление буфера обмена, звука, микрофона, камеры, принтеров и папок;
- встроенный SSH-терминал, системные SSH-ключи и локальные/удалённые/Dynamic
  SSH-туннели;
- двухпанельный SFTP с поиском, очередью передач, drag-and-drop, редактором
  POSIX-прав и сохранением подключения между вкладками;
- профили хранятся локально, секреты — в macOS Keychain.

## Быстрая установка из исходников

Требуются macOS 14 или новее, Xcode Command Line Tools и Homebrew.

```bash
brew install freerdp sdl3
git clone https://github.com/PastFly/Selective-Remote.git
cd Selective-Remote
./scripts/build_and_install.sh
```

Скрипт запускает тесты, собирает приложение, сохраняет предыдущую установленную
версию в Корзине и устанавливает `Selective Remote.app` в `/Applications`.

## Разработка

```bash
swift test
swift run SelectiveRemote
```

Полная distributable-сборка приложения и DMG:

```bash
./scripts/build_app.sh
```

Подробности: [сборка и проверка](BUILD-RU.md),
[участие в разработке](CONTRIBUTING.md), [политика конфиденциальности](PRIVACY.md)
и [публикация релизов](docs/RELEASING-RU.md).

## Разрешения macOS

Доступ к микрофону, камере и пользовательским папкам нужен только для их
перенаправления. Отказ от любого из этих разрешений не должен блокировать
обычное RDP-подключение.

## Лицензия

Исходный код Selective Remote распространяется по [MIT License](LICENSE).
Компоненты FreeRDP, SDL и xterm.js сохраняют собственные лицензии; см.
[уведомления о сторонних компонентах](Resources/THIRD-PARTY-NOTICES.txt).

## История изменений

### Исправлено в Public preview v9.2

- левый Command→Ctrl и левый Option→Windows снова передаются штатным механизмом
  FreeRDP, поэтому ⌘C/⌘V и Option работают в RDP;
- одновременно используется не больше двух официально поддерживаемых `remap`,
  поэтому исправление не возвращает системный код 22;
- проект подготовлен к открытой публикации: MIT, CI, документация, шаблоны
  issues/PR и проверка на приватные данные;
- версия приложения: **0.17.1 (build 65)**.

## Исправлено в Productivity v9.1

- включение **Option работает как Windows** больше не добавляет новые правила
  в `/kbd`: FreeRDP всегда получает ровно один служебный `remap`, поэтому его
  парсер не завершает RDP с системным кодом 22;
- Command, Option и собственные правила клавиатуры теперь преобразуются в
  безопасном AppKit/SDL-модуле до передачи событий FreeRDP;
- кнопки плавающей панели RDP получили постоянные видимые названия и подробные
  подсказки; переподключение, отключение и переключатели каналов также имеют
  понятные hover-подсказки;
- версия приложения: **0.17.1 (build 64)**.

## Добавлено в Productivity v9

- SFTP получил постоянную очередь передач: отдельный прогресс и скорость для
  каждого объекта, паузу, продолжение, отмену, повтор с `reget`/`reput`, а также
  правила конфликтов **сохранить оба / заменить / пропустить**; очередь не
  останавливается при переходе между SFTP, терминалом и другими вкладками;
- добавлена плавающая панель активных RDP-сессий, доступная поверх полноэкранных
  Spaces: Windows, смена языка, Alt+Tab, Ctrl+Alt+Delete, Print Screen,
  полный экран/окно, переподключение и отключение;
- звук, микрофон и камера меняются из панели и безопасно применяются автоматическим
  переподключением, поскольку FreeRDP создаёт эти каналы при старте сессии;
- правила клавиатуры теперь настраиваются по профилю: Command→Ctrl,
  правый Command→Windows, Option→Windows, Fn→Win+Space и собственные
  переназначения служебных клавиш;
- добавлены фиксированное окно и окно с динамическим разрешением FreeRDP;
- меню macOS показывает избранные профили для быстрого подключения, команды
  активных RDP-сессий и управление SFTP-передачами;
- управление RDP передаётся отдельному процессу через приватный FIFO и штатную
  очередь SDL; внутренние функции FreeRDP по-прежнему не перехватываются;
- версия приложения: **0.17.1 (build 63)**.

## Добавлено в Fn/permissions v8

- одиночное нажатие **Fn** в активном RDP-окне отправляет **Win+Space** и
  переключает раскладку Windows; сочетания Fn с другими клавишами не запускают
  переключение;
- новый Fn-модуль не перехватывает функции FreeRDP: он добавляет обычные
  клавиатурные события в штатную очередь SDL, а сборка отдельно запрещает
  появление таблицы FreeRDP/DYLD interpose в этом модуле;
- отказ macOS в доступе к микрофону или камере больше не завершает RDP-процесс:
  подключение продолжается без соответствующего канала;
- недоступные папки не передаются в Windows и не мешают запуску рабочего стола;
- версия приложения: **0.17.1 (build 62)**.

## Исправлено в RDP recovery v7

- удалён экспериментальный Fn input-модуль, после которого RDP-сеанс мог
  зависнуть сразу после создания окна;
- восстановлен проверенный путь запуска RDP из рабочего патча v4;
- SFTP, SSH-туннели, Retina/fullscreen и переназначение ⌘→Ctrl сохранены;
- версия приложения: **0.17.1 (build 61)**.

## Исправлено в патче Retina/keyboard v4

- смешанная связка Full HD + Retina больше не применяет глобальный
  `FREERDP_WLROOTS_HACK` к настоящим окнам RDP: скрытые monitor-probe получают
  логические размеры отдельно, а полноэкранные окна сохраняют собственную
  плотность пикселей каждого дисплея;
- добавлен профильный переключатель **Использовать ⌘ как Ctrl в Windows**,
  включённый по умолчанию; обе клавиши Command передаются как Ctrl, поэтому
  ⌘C/⌘V/⌘X/⌘A работают привычным для macOS способом;
- старые профили автоматически получают новое поведение, а переключатель можно
  отключить, чтобы снова передавать Command как клавишу Windows.

**Selective Remote** — самостоятельное нативное приложение macOS для RDP-, SSH-
и SFTP-профилей. RDP поддерживает визуальный выбор произвольного набора
физических дисплеев, а SSH — встроенный терминал, двухпанельный SFTP, системные
ключи и port forwarding. Пакет приложения называется `Selective Remote.app`,
исполняемый файл и служебные артефакты используют единое имя `SelectiveRemote`.

## Исправлено в 0.17.1

- завершено внутреннее переименование: `.app`, bundle identifiers, исполняемые
  файлы, helpers, журналы и настройки больше не используют прежнее имя;
- SFTP сохраняет подключение, текущие каталоги и фильтры при переходе между
  вкладками **SFTP** и **Терминал**;
- добавлены фильтры по имени, видимый столбец даты изменения, создание пустых
  файлов и папок через контекстное меню пустой области;
- одиночный щелчок явно выделяет файлы и папки, а редактор POSIX-прав получил
  понятные галочки чтения, записи и выполнения для владельца, группы и остальных;
- перенос удалённых файлов и папок использует ленивое файловое представление
  штатного `NSItemProvider`, совместимое со SwiftUI drag-and-drop;
- устранена ложная надпись «Удалённая папка пуста», когда OpenSSH возвращает
  непустой каталог в варианте `ls -l`, отличающемся от прежнего строгого шаблона;
- SFTP-парсер теперь принимает числовой или неизвестный link count, UID/GID
  вместо имён, ACL-маркеры, ведущие пробелы, локализованные месяцы, традиционную
  и ISO-дату, сохраняя пробелы в именах файлов; полные пути из ответа OpenSSH
  корректно приводятся к именам объектов текущего каталога;
- если будущая версия сервера вернёт действительно неизвестный формат,
  приложение покажет диагностическую ошибку с безопасным фрагментом ответа,
  а не выдаст непустую папку за пустую;
- добавлены регрессионные тесты на каталог Debian `/etc`, приглашение OpenSSH,
  ACL-маркеры, неизвестный link count и ISO-дату;
- версия приложения: **0.17.1 (build 58)**.

## Добавлено и изменено в 0.17.0

- SFTP превращён в полноценный двухпанельный проводник: появились история
  **назад/вперёд**, кликабельные хлебные крошки и адресная строка для прямого
  перехода к локальному или серверному пути;
- в списках сразу видны владелец с группой, символьные POSIX-права и размер;
  сортировка работает по имени, размеру, дате изменения, владельцу и доступу,
  папки при этом остаются сверху;
- правый клик открывает контекстное меню: открыть, открыть с помощью, встроенно
  редактировать удалённый UTF-8-файл, скопировать между панелями, переименовать,
  показать в Finder, удалить и открыть свойства;
- окно свойств показывает полный путь, тип, размер, дату, владельца, группу,
  символьный и восьмеричный режим; из него можно выполнить `chmod`, а при наличии
  серверных прав — сменить UID/GID через `chown` и `chgrp`;
- локальное удаление отправляет объект в Корзину; серверное удаление требует
  отдельного подтверждения и для папок допускается только при пустом каталоге;
- встроенный редактор ограничен текстовыми UTF-8-файлами до 5 МБ и при сохранении
  не переносит права временного файла на сервер;
- начат совместимый ребрендинг: видимое имя — **Selective Remote**, подпись —
  **RDP · SSH · SFTP**, акцент смещён к сине-бирюзовой палитре и сетевому знаку;
- версия приложения: **0.17.0 (build 57)**.

## Исправлено в 0.16.1

- устранена ошибка компиляции Xcode 26.5: внутреннее состояние прозрачности больше не конфликтует с унаследованным `NSView.appearance`;
- добавлен регрессионный тест, запрещающий повторное объявление конфликтующего свойства;
- темы, прозрачность окна и исправление `ssh-agent` из 0.16.0 сохранены без изменения функциональности;
- версия приложения: **0.16.1 (build 56)**.

## Добавлено и исправлено в 0.16.0

- набор терминальных тем расширен до 14 вариантов: добавлены Tokyo Night, Nord, One Dark, Gruvbox Dark, Catppuccin Mocha, Monokai, Rosé Pine и Solarized Light;
- оформление теперь открывается не только из терминала, но и глобальной кнопкой с палитрой в боковой панели;
- добавлен режим прозрачного окна с системным размытием и сохраняемой настройкой непрозрачности от 55 до 100%;
- SFTP и forwarding больше не требуют повторно загружать выбранный ключ в `ssh-agent`, если для профиля уже работает встроенная SSH-сессия: используется её защищённый OpenSSH ControlPath;
- при запуске SelectiveRemote из Finder без доступного `SSH_AUTH_SOCK` приложение ищет агент пользовательской launchd-сессии, а затем при необходимости запускает собственный `/usr/bin/ssh-agent`;
- сокет резервного агента создаётся в уникальном каталоге с правами `0700`, передаётся только дочерним SSH/SFTP-процессам и удаляется при завершении приложения;
- устранено предупреждение Swift 6.3 о доступе к `NSColorWell.color` вне MainActor;
- версия приложения: **0.16.0 (build 55)**.

## Исправлено в 0.15.1

- устранено падение Apple Swift 6.3.3 `SmallVector unable to grow` на этапе IRGen файла `TerminalAppearance.swift`;
- пользовательские цвета терминала теперь выбираются нативным `NSColorWell` и сохраняются непосредственно в формате `#RRGGBB`, без проблемного bridge `SwiftUI.Color → NSColor`;
- функциональность тем, пользовательских цветов, полноразмерного терминала и исправленной геометрии PTY из 0.15.0 сохранена;
- версия приложения: **0.15.1 (build 54)**.

## Добавлено и исправлено в 0.15.0

- терминал больше не находится внутри ограниченного `ScrollView`: он растягивается на всю доступную ширину и высоту окна;
- кнопка **Развернуть терминал** включает режим «Фокус»: скрываются боковая панель, заголовок профиля, вкладки и нижняя строка; повторное нажатие или `⌘⇧Enter` возвращает интерфейс;
- размер xterm пересчитывается после фактической раскладки SwiftUI/WKWebView и изменения шрифта, а PTY получает стабильные столбцы и строки;
- промежуточная геометрия `2×1`, возникающая во время переключения вкладок, больше не передаётся `nano`, `vim`, `tmux` и другим TUI-программам;
- ANSI/VT-вывод передаётся в WebView строго по порядку небольшими блоками и последовательно разбирается xterm.js; это устраняет повреждение полноэкранной разметки и цветов;
- локальная CSP терминала разрешает динамические стили, которые xterm.js 6 использует для canvas/DOM-геометрии; скрипты и внешняя сеть остаются запрещены;
- в панели **Оформление** доступны темы Midnight, Hacker Green, Solarized Dark, Dracula, Light и пользовательская тема;
- настраиваются фон, цвет текста, курсор, шрифт, размер, межстрочный интервал, форма и мигание курсора, внутренний отступ;
- настройки терминала сохраняются для следующих запусков и применяются также к отдельному терминалу генерации SSH-ключа;
- строка состояния терминала показывает фактический размер PTY в столбцах и строках;
- системное хранилище ключей ищется через LaunchServices по bundle identifier, затем по путям Keychain Access для разных macOS; при отсутствии отдельной «Связки ключей» используется приложение «Пароли»;
- отсутствие внешнего GUI хранилища больше не выдаётся за неисправность Keychain: SSH-ключами SelectiveRemote можно управлять во встроенном окне;
- версия приложения: **0.15.0 (build 53)**.

## Исправлено в 0.14.1

- устранена ошибка компиляции macOS: `NSFilePromiseProvider` больше не выдаётся за `NSItemProvider`;
- выгрузка удалённых файлов и папок в Finder теперь использует ленивую файловую репрезентацию штатного `NSItemProvider`;
- служебная копия загружается по SFTP только после фактического drop и удаляется из временного каталога позднее;
- внутренний drag-and-drop между удалённой и локальной панелями сохраняет отдельный безопасный payload;
- версия приложения: **0.14.1 (build 52)**.

## Добавлено и исправлено в 0.14.0

- SFTP получил двухпанельный файловый менеджер: слева локальный Mac, справа удалённый сервер;
- файлы и папки можно переносить между панелями кнопками или drag-and-drop, включая загрузку из Finder и экспорт удалённых объектов обратно в Finder;
- для удалённых папок используются рекурсивные операции SFTP, а новые локальные копии получают безопасное свободное имя вместо незаметной перезаписи;
- локальная панель умеет переходить домой, вверх, в выбранную через системный диалог папку, обновляться и создавать каталоги;
- SFTP и port forwarding могут повторно использовать уже аутентифицированную парольную SSH-сессию через отдельный защищённый OpenSSH control socket; пароль по-прежнему не сохраняется;
- сокет привязан к профилю, host, пользователю и порту, поэтому после изменения подключения старая сессия не будет использована для другого сервера;
- генерация SSH-ключа выполняется в отдельном встроенном PTY и больше не требует завершать текущую SSH-сессию;
- добавлены регрессионные тесты multiplexing и рекурсивных команд SFTP;
- версия приложения: **0.14.0 (build 51)**.

## Исправлено в 0.13.2

- удалён зависающий во время сборки диалоговый запуск системного `ssh-keygen`;
- PTY проверяется собственным детерминированным helper-процессом: тест синхронно проверяет вывод, ввод, Enter и изменение размера без внешних диалогов;
- аргументы реальной генерации SSH-ключа теперь отдельно проверяются Swift-тестом: passphrase по-прежнему вводится только во встроенном терминале и отсутствует в аргументах процесса;
- в журнал сборки добавлены явные сообщения до и после PTY-проверки;
- версия приложения: **0.13.2 (build 50)**.

## Исправлено в 0.13.1

- устранено ложное падение нативного PTY-теста после успешной Release-сборки: тест теперь читает ожидаемый вывод до ожидания завершения дочернего процесса и не попадает в гонку с неблокирующим PTY macOS;
- каталог встроенного xterm.js явно исключён из ресурсов SwiftPM, поскольку `build_app.sh` самостоятельно копирует его в bundle; предупреждения о восьми необработанных файлах больше нет;
- функциональность приложения не изменена;
- версия приложения: **0.13.1 (build 49)**.

## Добавлено в 0.13.0

- SSH больше не передаётся macOS как ссылка `ssh://`: Termius, Terminal и другие внешние обработчики не запускаются;
- в SelectiveRemote встроен полноценный терминальный экран на xterm.js 6.0.0 с цветом, Unicode, прокруткой, копированием, вставкой и изменением размера;
- системный `/usr/bin/ssh` запускается напрямую, без shell-строк, внутри собственного PTY; работают интерактивные password/passphrase/host-key запросы и TUI-программы;
- добавлена безопасная генерация ключей **Ed25519** и **RSA 4096** через системный `ssh-keygen`; путь выбирается заранее, существующие файлы никогда не перезаписываются;
- passphrase нового ключа вводится только в PTY: он отсутствует в аргументах процесса, окружении, профиле и журнале;
- выбранный публичный ключ можно интерактивно добавить на сервер через системный `ssh-copy-id`; пароль сервера вводится во встроенном терминале;
- окно **Keychain и SSH-ключи** получило команды создания и установки ключа;
- активные встроенные SSH-сессии отображаются в списке профилей, завершаются из строки состояния, контекстного меню и меню приложения;
- SFTP, port forwarding и весь RDP-контур 0.12.0 сохранены;
- версия приложения: **0.13.0 (build 48)**.

## Добавлено в 0.12.0

- отдельные типы профилей **RDP** и **SSH** с автоматической миграцией всех старых профилей в RDP;
- безопасный запуск интерактивного SSH в системном Terminal через `ssh://`, без shell-строк и без сохранения пароля сервера;
- настройки SSH-порта, пользователя, сжатия, keepalive, начальной папки SFTP и политики проверки host key;
- встроенный SFTP-браузер: навигация по папкам, обновление, загрузка, скачивание и создание каталогов;
- local (`-L`), remote (`-R`) и dynamic SOCKS (`-D`) forwarding; одновременно можно держать несколько туннелей, каждый со своим журналом;
- окно **Keychain и SSH-ключи**: регистрация существующего приватного ключа, fingerprint, переименование, показ в Finder, копирование `.pub` и управление `ssh-agent`;
- passphrase передаётся штатному `ssh-add --apple-use-keychain` через отдельный защищённый askpass-helper и затем загружается через `--apple-load-keychain`;
- профили и туннели используют системные `~/.ssh/config`, `known_hosts` и `ssh-agent`; собственный SSH-стек и сторонние сетевые библиотеки не добавлялись;
- экспорт профилей включает параметры SSH/SFTP/forwarding, но не включает пароли, passphrase, приватные ключи или привязку к локальному файлу ключа;
- сохранены без изменений весь RDP-контур 0.11.1, выбор дисплеев, Retina/fullscreen, камера, микрофон, качество и переподключение;
- версия приложения: **0.12.0 (build 47)**.

## Исправлено в 0.11.1

- устранено ложное окно ошибки «FreeRDP за 8 секунд не начал инициализацию», когда RDP-сессия уже открылась;
- готовность определяется не только по `Local framebuffer format` в журнале, но и по реальному окну процесса **Selective Remote Session** в WindowServer;
- обнаружение работает для полноэкранного окна на другом macOS Space и не требует разблокировки буферизованного INFO-вывода FreeRDP;
- небольшие системные и служебные диалоги не выдаются за готовое RDP-окно;
- настоящий долгий запуск теперь отображается ненавязчивым статусом, а не модальной ошибкой, пока процесс FreeRDP остаётся активным;
- источник определения готовности записывается в session-лог для последующей диагностики;
- добавлен регрессионный тест по фактическому размеру окна из проблемной сессии;
- версия приложения: **0.11.1 (build 46)**.

## Добавлено в 0.11.0

- в разделе **Дисплеи → Качество RDP-сессии** появился отдельный сетевой профиль для каждого подключения;
- доступны режимы **Автоматически**, **Макс. качество**, **Сбалансировано** и **Экономия**;
- явные режимы используют штатные параметры FreeRDP `/network:lan`, `/network:broadband-high` и `/network:broadband-low`, которые подбирают визуальные эффекты под пропускную способность;
- режим **Автоматически** не добавляет новый аргумент и полностью сохраняет поведение профилей из предыдущих версий;
- выбранный режим сохраняется при экспорте архива SelectiveRemote, а старые профили мигрируют автоматически;
- фактический режим и переданный параметр сети записываются в начало журнала каждой сессии;
- добавлены регрессионные тесты миграции и точного преобразования всех четырёх режимов в аргументы SDL-FreeRDP;
- версия приложения: **0.11.0 (build 45)**.

## Добавлено в 0.10.0

- в разделе **Устройства и звук** появилась проверка камеры и микрофона до запуска RDP;
- предпросмотр открывает именно источник, выбранный в текущем профиле, и применяет соответствующий режим качества;
- если сохранённая камера недоступна, окно проверки заранее показывает фактическую резервную камеру и предупреждает о переходе;
- отдельно отображаются статусы разрешений macOS для камеры и микрофона, активный системный микрофон и количество найденных камер;
- доступ можно запросить заранее, а при запрете — одним нажатием открыть нужный раздел системных настроек;
- предпросмотр запускается только по явному нажатию, ничего не записывает и освобождает камеру при остановке или закрытии окна;
- проверка блокируется, пока запущена хотя бы одна RDP-сессия, чтобы локальный предпросмотр не конкурировал с RDPECAM за устройство;
- AVFoundation-сеанс изолирован на последовательной очереди, поэтому запуск, перенастройка и остановка камеры не пересекаются;
- добавлены тесты преобразования TCC-статусов и соответствия профилей качества форматам предпросмотра;
- версия приложения: **0.10.0 (build 44)**.

## Исправлено в 0.9.1

- устранён аварийный `Abort trap: 6` с маркером `__TCC_CRASHING_DUE_TO_PRIVACY_VIOLATION__` при первом запросе доступа к микрофону;
- описание использования микрофона и entitlement Audio Input теперь есть как у внешнего **Selective Remote**, так и у вложенного **Selective Remote Session**: macOS может назначить ответственным за запрос любой из этих bundle в зависимости от способа запуска и подписи;
- скрипт сборки проверяет непустые описания микрофона и камеры в обоих `Info.plist`, а после подписи проверяет Camera и Audio Input entitlements в обоих приложениях; неполный DMG больше не создаётся;
- TCC-abort классифицируется по последнему активному запросу и показывает правильную подсказку отдельно для микрофона и камеры;
- версия приложения: **0.9.1 (build 43)**.

## Добавлено в 0.9.0

- в каждом профиле можно выбрать встроенную камеру Mac, системную камеру macOS или конкретное устройство — включая USB- и Continuity-камеры iPhone;
- RDPECAM передаёт Windows только выбранную камеру, поэтому удалённая программа больше не выбирает случайно первое устройство по хешу;
- если сохранённая камера недоступна, используется встроенная камера Mac, затем системная; причина перехода явно записывается в журнал;
- добавлены четыре режима качества камеры: **Экономия** (640×480, 15 FPS, цель 0,7 Мбит/с), **Сбалансированное** (1280×720, 30 FPS, цель 1,25 Мбит/с), **Высокое** (1920×1080, 30 FPS, цель 2,7 Мбит/с) и **Автоматически**;
- фиксированные режимы передают удалённой стороне один лучший поддерживаемый камерой формат в заданных пределах и задают целевой битрейт H.264; фактический трафик может отклоняться в зависимости от кодировщика, изображения и сервера;
- список камер автоматически обновляется при подключении и отключении устройств; доступно также ручное обновление;
- интерфейс показывает фактическое имя и тип выбранной камеры, а журнал — безопасный идентификатор, имя, тип, режим выбора, качество и согласованный формат;
- NV12-буфер кадра теперь выделяется один раз на поток и переиспользуется вместо создания нового массива до 3 МБ для каждого кадра 1080p;
- устаревший общий вызов перечисления AVFoundation заменён на `AVCaptureDeviceDiscoverySession`, Continuity Camera объявлена штатным типом устройства;
- старые профили мигрируют без действий пользователя: камера по умолчанию — встроенная, качество — сбалансированное;
- версия приложения: **0.9.0 (build 42)**.

## Исправлено в 0.8.10

- перед запуском FreeRDP вложенное приложение **Selective Remote Session** теперь само проверяет доступ к микрофону и камере и, если нужно, ожидает ответа в системном диалоге;
- первый запуск с включённым микрофоном больше не отменяется строкой `audin_mac_close: not authorized`, пока пользователь отвечает на запрос macOS;
- отказ, системное ограничение и тайм-аут разрешения распознаются отдельно для микрофона и камеры и показывают точный путь к нужному разделу настроек;
- открытый системный privacy-диалог больше не выдаётся за зависание FreeRDP; восьмисекундный контроль запуска начинается заново после выдачи разрешений;
- предварительная проверка загружается только для профилей с включённым микрофоном или доступной камерой и не затрагивает обычные RDP-сессии и определение мониторов;
- ненужные privacy-описания удалены из основного приложения: разрешения принадлежат только фактическому потребителю устройств — **Selective Remote Session**;
- добавлены тесты конфигурации preflight и классификации реальной ошибки `audin`;
- версия приложения: **0.8.10 (build 41)**.

## Изменено в 0.8.9

- состояние **«Подключено»** теперь устанавливается только после создания удалённого framebuffer, а не по первой служебной строке журнала;
- предупреждение о долгом запуске автоматически снимается, если удалённый рабочий стол всё же успешно открылся;
- одиночный `ERRCONNECT_CONNECT_CANCELLED` больше не скрывается как штатное завершение без подтверждения `Connection aborted by user`;
- список SDL-мониторов кэшируется до изменения конфигурации дисплеев, поэтому запуск следующих параллельных сессий не выполняет лишний `/list:monitor`;
- наличие camera addin определяется один раз вместо повторного обхода каталога Frameworks при каждом обновлении SwiftUI;
- запись профилей в UserDefaults объединяется с задержкой 300 мс, поэтому ввод текста больше не сериализует весь список на каждое нажатие клавиши; при завершении приложения данные сохраняются немедленно;
- поиск профилей больше не создаёт четыре временные lowercase-строки на каждый профиль;
- сортировка вертикально расположенных мониторов с одинаковой координатой X стала детерминированной;
- удалены неиспользуемые `DisplayDescriptor.scaleFactor`, `DisplayPlacement.sourceFrame` и импорт `ColorSync`;
- чтение вывода `/list:monitor` больше не может заблокироваться из-за заполненного pipe;
- проверка обновлений учитывает `minimumMacOS` и не предлагает несовместимый пакет;
- версия приложения: **0.8.9 (build 40)**.

## Исправлено в 0.8.8

- исправлена первопричина обрезанной панели Windows на единственном Retina-дисплее: глобальный `FREERDP_WLROOTS_HACK=force` больше не подменяет реальный размер fullscreen drawable логическими границами экрана;
- вместо отключения Retina backing используется штатный `/smart-sizing:<ширина>x<высота>` SDL-FreeRDP: удалённый рабочий стол целиком масштабируется в фактическую область полного экрана, включая панель **Пуск**;
- верхнее меню macOS снова доступно, а interposer больше не вмешивается в создание и переключение полноэкранного окна при одном дисплее;
- topology interposer и monitor-probe fallback сохранены только для выбранных многомониторных схем;
- версия приложения: **0.8.8 (build 39)**.

## Почему камера не работала в 0.7.1

В исходниках были файлы RDPECAM и AVFoundation, но production-сборка намеренно их не компилировала. Переключатель камеры был заблокирован, а в аргументы FreeRDP не добавлялся `/dvc:rdpecam`. Поэтому Windows не получала ни одного видеоустройства, хотя сама RDP-сессия работала.

Начиная с версии 0.8.3 это устранено без возврата опасного глобального перехвата загрузчика: channel addin и AVFoundation HAL собраны в один изолированный модуль `librdpecam-client`, который загружается штатным API FreeRDP.

## Микрофон: условия работы

1. В профиле включите **Микрофон**.
2. Нажмите **Проверить устройства…** и заранее убедитесь, что выбран нужный системный микрофон и доступ разрешён.
3. При первом запросе macOS укажет **Selective Remote** или **Selective Remote Session**. Выберите **Разрешить**.
4. Если доступ ранее был отклонён, окно диагностики откроет раздел **Системные настройки → Конфиденциальность и безопасность → Микрофон**.

## Камера: условия работы

1. Собирайте именно полный `.app`/DMG через `scripts/build_app.sh`; запуск `swift run` не содержит упакованного camera addin.
2. В профиле откройте **Устройства и звук → Камера**, включите перенаправление, выберите источник и качество. Для первой проверки рекомендуется **Встроенная камера Mac** и **Сбалансированное**.
3. Нажмите **Проверить устройства…** и запустите локальный предпросмотр. В строке под ним будет указано фактически открытое устройство и формат.
4. Полностью переподключите RDP: изменение камеры или качества не перестраивает уже активный поток.
5. При первом использовании подтвердите системный запрос. Если доступ ранее отклонён, окно диагностики откроет нужный раздел настроек.
6. На удалённом Windows-компьютере политика **Do not allow video capture redirection** должна быть **Disabled** или **Not configured**:
   `Computer Configuration → Administrative Templates → Windows Components → Remote Desktop Services → Remote Desktop Session Host → Device and Resource Redirection`.
7. После изменения политики выполните `gpupdate /force`, завершите удалённую сессию полностью и подключитесь снова.

В session-логе исправная инициализация содержит строки `Loading Dynamic Virtual Channel rdpecam`, `Selected camera`, `Enumerated 1 selected macOS camera` и `Camera started`.

## Сохранённые возможности

Все возможности RDP сохранены: ручная виртуальная раскладка, сон и hot-plug мониторов, импорт/экспорт без паролей, отдельный Keychain для RD Gateway, поиск и группы, параллельные сессии, направления clipboard, масштаб Windows 100/140/180%, обновления, управление сессиями и переносимый Drag & Drop DMG.

## Оформление

Нажмите кнопку с палитрой рядом с названием Selective Remote в боковой панели или
такую же кнопку над терминалом. В одном окне доступны 14 тем терминала,
пользовательские цвета, шрифт и курсор, а также переключатель прозрачности
всего окна. Ползунок **Непрозрачность** ограничен диапазоном 55–100%, чтобы
элементы управления оставались читаемыми. Все параметры сохраняются
автоматически.

## Первое SSH-подключение

1. Нажмите **+ → Новое SSH**.
2. Укажите hostname или имя `Host` из `~/.ssh/config`, пользователя и порт.
3. Для обычной парольной авторизации оставьте ключ **Автоматически** и нажмите **Открыть SSH** — приложение перейдёт на вкладку **Терминал**, где OpenSSH запросит пароль.
4. Для существующего ключа нажмите **Добавить ключ…** и выберите приватный файл.
5. Для нового ключа нажмите **Создать ключ…**, выберите Ed25519 или RSA 4096 и путь. Passphrase дважды вводится в отдельном встроенном терминале; пустой passphrase допустим. Уже открытая SSH-сессия продолжит работать.
6. Нажмите **Установить на сервер**, подтвердите host key и введите текущий пароль SSH. После успешной установки подключитесь снова уже по ключу.
7. Для SFTP и фоновых туннелей можно оставить аутентифицированную SSH-сессию открытой. Альтернатива — выбрать ключ в профиле: при отсутствии доступного системного агента Selective Remote автоматически запустит собственный и безопасно запросит passphrase.

Selective Remote не копирует импортированный приватный ключ. Созданный ключ записывается системным `ssh-keygen` непосредственно по выбранному пути. В профиле хранится только внутренний идентификатор, имя, путь и fingerprint; файл остаётся под контролем пользователя.

## SFTP

На вкладке **SFTP** нажмите **Подключить SFTP**. Слева отображается локальный
Mac, справа — удалённый сервер. Доступны:

- переход назад/вперёд, вверх, по хлебным крошкам и напрямую по адресу;
- сортировка по имени, размеру, дате, владельцу и правам доступа;
- колонки владельца/группы, POSIX-прав и размера;
- контекстное меню, свойства, переименование и безопасные подтверждения удаления;
- встроенное редактирование удалённых UTF-8-файлов до 5 МБ;
- открытие локальных и временных удалённых копий в выбранном приложении;
- перенос файлов и папок между панелями кнопками или drag-and-drop;
- загрузка файлов и папок из Finder на сервер;
- выгрузка удалённых файлов и папок непосредственно в Finder;
- рекурсивная передача каталогов;
- создание папок на Mac и на сервере.

SFTP работает в `BatchMode` и использует выбранный ключ, системный/резервный
`ssh-agent` либо control socket уже аутентифицированной встроенной SSH-сессии. Для парольного
входа сначала подключитесь во вкладке **Терминал** и оставьте её открытой:
повторно вводить пароль в SFTP не потребуется. Поле **Начальная папка SFTP**
поддерживает `.`, абсолютный путь и путь домашнего каталога, разрешённый
сервером.

## Port Forwarding

На вкладке **Туннели** добавьте один из режимов:

- **Локальный** — порт на Mac перенаправляется к host:port со стороны SSH-сервера;
- **Удалённый** — порт на SSH-сервере перенаправляется к host:port со стороны Mac;
- **SOCKS** — создаётся локальный SOCKS5-прокси.

Туннели запускаются с `BatchMode=yes`, `ExitOnForwardFailure=yes` и keepalive. Изменять активное правило нельзя; сначала остановите его. Remote forwarding зависит от серверных `AllowTcpForwarding` и `GatewayPorts`.

## Честное ограничение без Developer ID

Community-сборка использует ad-hoc подпись либо локальный сертификат Apple Development. Она проверяет собственную целостность, но macOS не может подтвердить личность публичного издателя и notarization выполнить нельзя.

Поэтому на новом Mac пользователь должен **один раз явно разрешить запуск**: после копирования открыть папку **Программы**, щёлкнуть `Selective Remote` правой кнопкой и выбрать **Открыть**. Если macOS всё равно блокирует запуск, используйте **Системные настройки → Конфиденциальность и безопасность → Всё равно открыть**. Глобальные настройки Gatekeeper изменять не нужно.

Полностью обычный первый запуск двойным щелчком без такого подтверждения возможен только после подписи **Developer ID Application** и notarization у Apple. Скрипт уже поддерживает этот путь, если сертификат появится позже.

## Что получает пользователь

В видимой части community DMG находятся только:

- `Selective Remote.app`;
- ярлык `Applications`.

### Установка и переустановка

1. Пользователь открывает DMG.
2. Перетаскивает `Selective Remote` по стрелке на `Applications`.
3. При обновлении выбирает **Заменить**.
4. Извлекает DMG и запускает установленную копию из папки **Программы**.

Перед заменой необходимо закрыть Selective Remote, активные RDP-сессии и SSH-туннели. Профили, журналы и записи Keychain находятся вне `.app`, поэтому при замене не удаляются.

### Удаление

Закройте Selective Remote и переместите `/Applications/Selective Remote.app` в Корзину.

## Архитектуры Mac

Сборка переносима между Mac одной архитектуры:

- Mac с Apple Silicon (включая M1–M4) создаёт `community-arm64.dmg`;
- Intel Mac создаёт `community-x86_64.dmg`.

Нельзя выдавать arm64 DMG за Intel-сборку. Если нужны обе платформы, соберите и опубликуйте два DMG с понятными именами. Модели, количество, разрешения и частоты мониторов при этом не зашиты в код.

## Требования только к Mac сборщика

- версия macOS, поддерживаемая установленными Homebrew-библиотеками;
- полный Xcode, выбранный через `xcode-select`;
- Homebrew;
- FreeRDP 3.x и SDL3.

```bash
brew install freerdp
```

XQuartz не нужен: используется SDL-клиент `sdl-freerdp`, а не X11-клиент `xfreerdp`. Все эти зависимости нужны только для создания DMG и вкладываются в готовое приложение.

Эффективный minimum deployment target определяется самой новой вложенной библиотекой. В вашем журнале Homebrew FreeRDP/SDL/FFmpeg собраны для macOS 26.0, поэтому созданный на этом Mac DMG будет требовать **macOS 26.0 или новее**, несмотря на то что собственный Swift-код поддерживает macOS 14. Для выпуска под macOS 14/15 зависимости нужно собирать на совместимом окружении с соответствующим deployment target. Скрипт печатает найденный минимум и записывает его в приложение; macOS проверит совместимость при запуске.

## Сборка community DMG

На вашем M4 Pro выполните:

```bash
cd ~/Downloads/SelectiveRemote-0.17.1-source
chmod +x scripts/build_app.sh
./scripts/build_app.sh
```

Успешная сборка создаёт:

- `dist/Selective Remote.app`;
- `dist/SelectiveRemote-0.17.1-community-arm64.dmg`;
- `dist/SelectiveRemote-0.17.1-community-arm64.dmg.sha256`.

Именно два последних файла можно распространять. Получателю не передаются исходники и не требуется запускать `build_app.sh`.

Скрипт выполняет Swift-тесты, нативный C++-тест interposer, smoke-тест Homebrew-клиента, Release-сборку, генерацию `.icns`, перенос библиотек, переписывание load commands, проверку отсутствия Homebrew-ссылок, подпись вложенного кода, portable smoke-тест и создание DMG.

## Проверка SHA-256

Перед публикацией сохраните строку из файла `.sha256`. Получатель может проверить скачанный DMG:

```bash
cd ~/Downloads
shasum -a 256 SelectiveRemote-0.17.1-community-arm64.dmg
```

Результат должен полностью совпасть с опубликованным. SHA-256 подтверждает совпадение файла с выложенной вами копией, но не заменяет удостоверение издателя Apple.

## Подпись и notarization в будущем

Приоритет сертификатов: явно заданный `SELECTIVEREMOTE_CODESIGN_IDENTITY`, затем **Developer ID Application**, затем **Apple Development**, затем ad-hoc подпись.

```bash
security find-identity -v -p codesigning

SELECTIVEREMOTE_CODESIGN_IDENTITY="Apple Development: Ваше имя (…)" \
  ./scripts/build_app.sh
```

Для доверенного публичного релиза после получения Developer ID один раз сохраните credentials:

```bash
xcrun notarytool store-credentials "SelectiveRemote-notary" \
  --apple-id "APPLE_ID" \
  --team-id "TEAM_ID" \
  --password "APP_SPECIFIC_PASSWORD"
```

Затем:

```bash
SELECTIVEREMOTE_CODESIGN_IDENTITY="Developer ID Application: Имя (TEAM_ID)" \
SELECTIVEREMOTE_NOTARY_PROFILE="SelectiveRemote-notary" \
SELECTIVEREMOTE_UPDATE_FEED_URL="https://example.com/updates.json" \
  ./scripts/build_app.sh
```

Скрипт включает Hardened Runtime, отправляет DMG через `notarytool --wait`, прикрепляет ticket командой `stapler` и проверяет Gatekeeper.

## Пароли и Keychain

- RDP и RD Gateway хранятся как две отдельные generic-password записи.
- Keychain читается только после нажатия **Подключиться**.
- Введённый пароль можно использовать один раз без сохранения.
- Пароли не входят в UserDefaults, журналы, `.rdp`, `.selectiveremote`, DMG или список процессов.
- Аргументы и секреты передаются FreeRDP через `/args-from:stdin`.
- пароль SSH-сервера не хранится и запрашивается системным OpenSSH во встроенном PTY;
- passphrase при генерации ключа вводится только во встроенном PTY и не передаётся через аргументы или окружение;
- passphrase приватного ключа обслуживает Apple OpenSSH через `ssh-add --apple-use-keychain`;
- askpass-helper печатает passphrase только в закрытый stdin/stdout-канал `ssh-add`; значение не передаётся в аргументах, окружении или журнале;
- SFTP и фоновые туннели используют системный или запущенный приложением
  `ssh-agent`, либо изолированный control socket активной встроенной SSH-сессии.

Фиксированный путь установки помогает избежать лишних изменений, но ad-hoc подпись не даёт стабильной криптографической личности между разными сборками. После обновления macOS может снова запросить разрешение Keychain. Гарантированно стабильное доверие между релизами требует постоянного сертификата подписи.

## Поддержка разных мониторов

Код не содержит названий конкретных моделей. macOS-дисплей хранится в профиле по UUID CoreGraphics и при запуске сопоставляется с SDL по совокупности признаков:

1. нормализованное имя;
2. логическое и физическое разрешение;
3. признак системного основного дисплея;
4. физический порядок экранов.

Это рассчитано на другие модели, ориентации, частоты и повторяющиеся разрешения. После постоянной замены монитора нажмите **Забыть недоступные** и выберите новый экран. При временном отключении UUID сохраняется, чтобы схема восстановилась после подключения.

## Импорт, сон и несколько сессий

- импортируются `.rdp`, `.selectiveremote` и `.json`;
- экспортируются RDP- и SSH-профили, параметры SFTP и forwarding, всегда без паролей, passphrase и локального ключа;
- выбранный `.rdp` экспортируется только из RDP-профиля;
- перед сном активные дочерние процессы завершаются контролируемо;
- при отключении используемого монитора завершаются только затронутые сессии;
- разные профили могут работать одновременно;
- конкретную или все сессии можно завершить из окна, контекстного меню, меню **Сессия** или строки меню macOS;
- **правый Shift + Enter** переключает полный экран/окно;
- **правый Shift + D** отключает RDP.

## Виртуальная схема мониторов

В автоматическом режиме физически несмежные выбранные дисплеи становятся соседними в Windows. В ручном режиме карточки перетаскиваются с шагом 20 px; основной дисплей нормализуется к `(0, 0)`. Разрешены отрицательные и вертикальные координаты, а перекрытие экранов блокирует запуск.

Interposer меняет только координаты выбранных `rdpMonitor`; системная раскладка macOS и Homebrew-пакеты не модифицируются.

## Лицензии встроенных компонентов

FreeRDP распространяется по Apache License 2.0, SDL — по zlib license, xterm.js и addon-fit — по MIT License. Во время сборки скрипт определяет Homebrew-формулу каждой вложенной dylib и копирует найденные `LICENSE`, `COPYING`, `COPYRIGHT` и `NOTICE` в `Contents/Resources/ThirdPartyLicenses`. Полные лицензии терминала находятся в `Contents/Resources/TerminalResources`.

Перед широким публичным распространением DMG проверьте этот каталог и условия точных версий всех зависимостей, особенно мультимедийных библиотек. Технически переносимый пакет не отменяет лицензионные обязанности распространителя.

## Диагностика

Кнопка **Журнал** открывает индивидуальный файл RDP-сессии в `~/Library/Application Support/SelectiveRemote/Logs`. У каждого SSH-туннеля есть отдельная кнопка журнала в том же каталоге. Параллельные процессы используют разные имена файлов.

Следующие сообщения сами по себе не означают сбой SelectiveRemote:

- `nsc_init_neon_int: TODO` — FreeRDP использует обычную реализацию NSCodec вместо ещё не реализованной NEON-оптимизации;
- предупреждения `experimental build options` и `runtime-check` описывают параметры сборки установленного Homebrew FreeRDP;
- `self-signed certificate` ожидаем при первом подключении к серверу с самоподписанным сертификатом в режиме TOFU;
- `pthread_setschedprio ... not implemented` означает, что macOS не применила необязательный приоритет потока;
- `ERRCONNECT_CONNECT_CANCELLED` вместе с явным `Connection aborted by user` считается штатным закрытием окна. Без этой строки код означает отменённое подключение и SelectiveRemote покажет диагностическую ошибку.

Строки `[SelectiveRemote Privacy] requesting ...` и `granted ...` означают штатную предварительную проверку. `denied`, `restricted` или `timeout` сопровождаются точной подсказкой в интерфейсе. Старый FreeRDP-маркер `audin_mac_close: not authorized` также распознаётся как отказ в доступе к микрофону.

Успешная инициализация удалённого рабочего стола содержит
`Local framebuffer format` или `Remote framebuffer format`. Для камеры дополнительно
появляются `Registered SelectiveRemote AVFoundation camera backend` и
`Enumerated N macOS camera(s)`.

Полную macOS-сборку необходимо выполнять на Mac: Linux-проверка подтверждает структуру Swift, shell и нативную логику, но не может линковать AppKit, Security, SDL3 и Homebrew FreeRDP и создать настоящий DMG.
